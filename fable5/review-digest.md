# Review digest (wf_aeae1114)
counts: {'blocker': 19, 'major': 94, 'minor': 80}

# BLOCKER

## [CLAUDE.md | CLAUDE.md] Routing table row 2 (line 10) + footer re-arm line (line 46)
ISSUE: Post-compaction deadlock: the footer says docs read before compaction no longer count and '(cached)' is invalid, and orders you to re-run the routing table. But row 2 fires only on 'your first Edit or Write of this session' — that event was consumed pre-compaction and can never occur again, so no row ever routes back to CODE.md. The model's memory of C1–C14 was just wiped by compaction, '(cached)' is banned, and the routing table gives no compliant path to re-read it. Result: every post-compaction edit runs unguarded by CODE.md for the rest of the session.
FIX: Row 2 -> '| are about to make your first Edit or Write since session start or the last compaction | docs/guardrails/CODE.md |', and mirror in CODE.md line 2 ('...your first Edit or Write since session start or the last compaction').

## [routing-sim | CLAUDE.md] Routing table, row 3 (line 11) — S1 simulation / adversarial 'no row fires' test
ISSUE: DEBUG.md is unreachable at the moment it matters most: a user-reported bug. In S1 ('Fix the crash in utils/parse.py when the input file is empty') no traceback exists in the transcript, so row 3 ('see a failing test, non-zero exit, traceback') never fires. The only row that fires is row 2 at the first Edit, routing to CODE.md. The reproduce-before-fix discipline presumably lives in DEBUG.md, but the model only reads DEBUG.md AFTER it sees a failure — and nothing tells it to reproduce first. A mid-tier model will patch the reported location unreproduced (iron rule at line 26 says treat the location as a hypothesis, but the doc holding the debugging procedure is never routed to).
FIX: Extend row 3: `| see a failing test, non-zero exit from a build/test/run command, a traceback (in output or pasted by the user), or the user reports a bug/crash/error you have not reproduced this session | docs/guardrails/DEBUG.md |` and make sure DEBUG.md line 1 restates the added clause.

## [routing-sim | CLAUDE.md] Footer line 46 vs routing contract line 17 — S6 simulation
ISSUE: Contradictory first-tool-call orders after compaction. Row 6 + the contract demand: write `TRIGGER: compaction -> docs/guardrails/SESSION.md` and make the NEXT tool call Read SESSION.md. The footer demands: 'Read docs/STATE.md, then re-run the routing table.' Both claim the next tool call. A literal mid-tier model cannot satisfy both; it will pick one (likely the footer, since it is the last thing in the file) and thereby violate the routing contract in its very first post-compaction action — training it that the contract is optional.
FIX: Make the footer defer to the router: `After compaction or /resume: routing row 6 has fired — write the TRIGGER line and Read docs/guardrails/SESSION.md (its checklist begins with Read docs/STATE.md). Docs read before compaction no longer count as read — (cached) is invalid until you Read the doc again.` Move the STATE.md read into SESSION.md's checklist item 1 if it is not already there.

## [MIGRATE.md | MIGRATE.md] M6a, lines 62-65 (collision gate vs. copy command)
ISSUE: The collision protocol is per-file ('Before each copy... Destination exists? ... ask the user: (a) rename, (b) skip this kit doc, (c) back up then replace') but the copy command is a wildcard: `cp <KIT>/docs/guardrails/*.md docs/guardrails/`. If any file collided and the user chose (a) or (b), the wholesale wildcard copy overwrites it anyway — destructive action directly contradicting a user decision made seconds earlier. A mid-tier model will run the printed command literally.
FIX: Replace the copy line with a per-file form: 'Copy each file that passed the gate individually: `cp <KIT>/docs/guardrails/<X>.md docs/guardrails/<X>.md` | `Copy-Item <KIT>/docs/guardrails/<X>.md docs/guardrails/<X>.md` — never use a wildcard copy when any destination file already exists.'

## [migration-dry-run | MIGRATE.md] M6a, lines 63-64 (collision protocol vs the Copy command)
ISSUE: The per-file collision protocol ('Before each copy: test -e ... hashes differ -> do NOT overwrite ... ask the user') is immediately followed by ONE bulk command: `cp <KIT>/docs/guardrails/*.md docs/guardrails/`. A literal executor runs the prechecks, asks about the team's existing docs/guardrails/VERIFY.md, and then the only copy command given clobbers it anyway — including after the user chose (b) skip. The procedure as written cannot honor its own protocol; on this project it destroys the team's release checklist.
FIX: Replace the Copy line with a per-file loop that consumes the collision decisions: 'Copy one file at a time, only names whose precheck passed or whose collision the user resolved (a)/(c): `mkdir -p docs/guardrails && for f in _FORMAT PLAN CODE DEBUG VERIFY EFFICIENCY SESSION; do cp "<KIT>/docs/guardrails/$f.md" docs/guardrails/; done` | PowerShell foreach equivalent. Remove any user-skipped name from the list and log it as SKIPPED under MIGRATION-LOG.md `## Kit-doc collisions`.'

## [migration-dry-run | MIGRATE.md] M3 example table row 003, line 40; disposition rules line 44
ISSUE: Example row 003 marks 'Always run lint before committing' as SUPERSEDED-BY 'docs/guardrails/VERIFY.md echo protocol | no project delta'. This is factually wrong against the shipped kit: VERIFY.md V1-V12 contain no lint step (PLAN.md P5 runs lint only as a one-time task-start baseline), so supersession silently deletes the pre-commit lint requirement. It also violates M3's own rule that SUPERSEDED-BY must 'name kit doc + rule ID' — 'echo protocol' is not an ID. Worse, the legacy file contains this exact line (its line 004), and M3 says 'clone the example's shape exactly': a literal Opus copies the disposition by lookalike and the rule dies.
FIX: Replace row 003 with a line the kit genuinely covers, with a real ID: `| 003 | Never say done without re-running the tests | SUPERSEDED-BY | docs/guardrails/VERIFY.md V1 | covered 100% |`. Append to the disposition rules: 'SUPERSEDED-BY is legal only after quoting the kit rule ID whose text covers 100% of the old line; anything the kit rule does not literally require is a delta per M4(b) or stays KEPT-VERBATIM. Example rows are SHAPE only — never copy a disposition because your line resembles an example.'

## [migration-dry-run | MIGRATE.md] M7 line 76 ('@' imports clause), M6b line 69 (zone-2 allowed content), M1 lines 19-24
ISSUE: Legacy line 012 is `@docs/api-conventions.md`. MIGRATE.md gives it no disposition rule: M1 never inventories @imports as instruction surfaces (the imported file is auto-loaded content); M6b's zone-2 allowed-content list (commands, constraints, pointers) excludes import lines, so M6c's catch-all routes it to PROJECT.md or PROJECT-NOTES.md — where @imports are inert, silently unloading docs/api-conventions.md every future session. M7's instruction to 'rewrite repo-root-relative' presumes an import survives the move; it does not (imports are processed only inside CLAUDE.md memory files, resolving relative to the containing file — the parenthetical admits this while the imperative contradicts it).
FIX: Three edits. M1, add a bullet: 'Grep `^@` in every CLAUDE.md found; each imported file is an instruction surface (decision usually LEAVE; the import LINE is migrated with the containing file).' M6b allowed content, append: '`@path` import lines carried from the original CLAUDE.md — imports are processed ONLY inside CLAUDE.md files; NEVER move an `@` line into docs/guardrails/* (it becomes inert text).' M7, replace the import clause with: '`@` imports stay in a CLAUDE.md file; if the containing CLAUDE.md's directory is unchanged, keep the path byte-identical; verify the target with `test -e <path>` | `Test-Path <path>`. Plain relative paths in prose moved to docs/guardrails/*: rewrite repo-root-relative.'

## [migration-dry-run | MIGRATE.md] M6c line 72, with kit CLAUDE.md routing table lines 7-15
ISSUE: M6c mandates moving style detail, debug lore, and environment quirks VERBATIM into docs/guardrails/PROJECT.md — but nothing in the entire kit ever routes there: no routing-table row, no reference from any kit doc (verified by grep), and zone-2 pointer lines are listed in M6b only as 'Allowed content', never required. On this project, legacy line 010 (no default exports; lowercase error strings) and line 011 (EPERM = OneDrive lock lore) go from always-loaded CLAUDE.md text to text no session ever reads at the moment it matters. M8's verbatim spot-check cannot catch this — the bytes survive; the reachability dies.
FIX: Append to M6c: 'For EVERY `##` anchor you create in docs/guardrails/PROJECT.md, add one zone-2 pointer line `<observable trigger> -> Read docs/guardrails/PROJECT.md#<anchor>` to CLAUDE.md. A PROJECT.md section with no zone-2 pointer is unreachable and counts as DROPPED — which requires user approval.' Add M8 item (9): 'Print each PROJECT.md `##` anchor beside its zone-2 pointer line; any anchor without a pointer = FAILED.'

## [CODE.md | CODE.md] ## REFERENCE SWEEP, steps 1-2 and closing line (lines 26-32)
ISSUE: REFERENCE SWEEP directly contradicts EFFICIENCY.md. Step 2 says 'Paste the hit list as file:line' and the closing line says 'Do not proceed... until every hit is dispositioned' — with no hit cap. EFFICIENCY.md E11 says a >50-hit search must NOT be scrolled or pasted, and E8 says 'any repo-wide sweep (... find-all-usages ...)' must be delegated — which describes every REFERENCE SWEEP. The CLAUDE.md routing row '>50 hits -> EFFICIENCY.md' fires mid-sweep and sends the model to a doc forbidding exactly what CODE.md just mandated. On any common identifier the two procedures deadlock and the model will rationalize one away.
FIX: Add a step between current steps 2 and 3: '2b. >50 hits: re-run with a word-boundary pattern (`\b<name>\b`) excluding vendored paths (node_modules/, dist/, vendor/); still >50 -> delegate the sweep per docs/guardrails/EFFICIENCY.md E8/E9 and disposition the returned list yourself.' (EFFICIENCY.md E8 also needs a carve-out — 'REFERENCE SWEEP under 50 hits stays in main context' — flag to that file's owner.)

## [fact-check | CODE.md] line 43, 'Dates & times' table: 'Format strings: yyyy, never YYYY (YYYY is ISO week-year — wrong near New Year).'
ISSUE: Overbroad and inverted for two of the most common JS date libraries. Probed with installed libs (node v24.14.1): dayjs `format('yyyy-MM-DD')` -> 'yyyy-01-15' (LITERAL 'yyyy' in output — a hard bug), dayjs/moment `format('YYYY-MM-DD')` -> '2024-01-15' (YYYY IS the correct calendar-year token there); moment's week-year tokens are gggg/GGGG (probed: moment('2024-12-30').format('gggg') -> '2025'). The rule as written is correct only for LDML-token libraries: date-fns (probed: format(d,'YYYY-MM-dd') throws RangeError 'Use `yyyy` instead of `YYYY`'), Java SimpleDateFormat/DateTimeFormatter, Luxon, ICU/Swift. Also a nit: in Java/date-fns uppercase Y is the LOCALE week-numbering year, not strictly 'ISO week-year'. A mid-tier model obeying this row in a moment/dayjs codebase will write lowercase yyyy and ship literal 'yyyy' strings.
FIX: Replace the row with: 'Format-string year token depends on the library: Java/date-fns/Luxon (LDML tokens): yyyy, never YYYY (uppercase = week-numbering year — wrong near New Year); Moment/Day.js: YYYY IS the calendar year (their week-year is gggg/GGGG — lowercase yyyy prints literally in Day.js); Python strftime: %Y (%G is ISO week-year). Unsure which family? Probe one late-December date (e.g. 2024-12-30) with both tokens and paste the output.'

## [DEBUG.md | DEBUG.md] D4 (line 9) vs D8 (line 13)
ISSUE: Procedural contradiction in the common re-fix scenario. D4 hard-caps 'Full suite at most twice per task: baseline and final.' D8 mandates a suite run after every landed fix ('(2) the nearest enclosing suite green'). If the D8 confirmation run reveals a new failure, the model re-enters the fix loop; after the second fix, D8 demands a third suite run, which D4 forbids. A literal mid-tier model either ships unconfirmed (obeying D4) or violates D4 (obeying D8) — and 'per task' is undefined for multi-bug tasks, so the cap is uncountable anyway.
FIX: Rewrite D4's cap sentence to be per-fix-cycle and point at D8 as the sanctioned run: 'Full suite only at two points: the baseline and the D8 confirmation — never inside the fix loop -> instead: re-run only the narrowed command. A failed confirmation opens a NEW fix cycle with its own confirmation run.'

## [EFFICIENCY.md | EFFICIENCY.md] E15, line 24 (conflict with docs/guardrails/VERIFY.md echo protocol, line 4 and V1-V12)
ISSUE: Direct inter-doc contradiction. VERIFY.md mandates one echo line per item ('V<n>: PASS|FAIL|N/A — command + output line' — 12 lines) plus an evidence table in the SAME reply where you write 'done'. E15 caps that reply at 10 lines. A mid-tier model resolves the contradiction in favor of less work and cites E15 to skip the V-echo — exactly the evidence the kit exists to force. Bonus defect on the same line: 'routine task' is a judgment word (F14) and is the escape hatch in the other direction ('this wasn't routine, so no cap').
FIX: Replace E15 with: '- E15. Final answer for a task where the user asked for changes (not an explanation/report): at most 10 lines — changed files as path:line + what changed, the verification command and observed result, open risks. VERIFY.md echo lines (V1..V12) and the evidence table are exempt from the cap. No code blocks unless the user must copy them; no restating the request; no narrating the journey.'

## [SESSION.md | SESSION.md] line 10 header ('create it at task start') + lines 4-8 (post-compaction steps)
ISSUE: The STATE.md lifecycle has two compounding holes. (1) Creation is gated on a judgment-state trigger: the only routing row that brings a model here at task start is 'the task will outlive this sitting' (CLAUDE.md line 14) — a prediction a mid-tier model will almost never make mid-work, so STATE.md predictably never gets created. (2) The recovery procedure has no missing-file branch: post-compaction step 1 is 'Read docs/STATE.md in full', which errors on a nonexistent file, and the doc says nothing about what to do then — the exact zero-context disaster scenario the kit exists to prevent dead-ends. PLAN.md never instructs creating STATE.md either, so there is no backstop.
FIX: Change line 10 to: '## No docs/STATE.md exists the moment this doc fires? Create it NOW with EXACTLY these sections'. Add to step 1: 'docs/STATE.md missing? Create it from the template below, filling Goal/Now from the compaction summary and `git log --oneline -10` + `git diff --stat`, marking every entry UNVERIFIED.' Cross-file: replace the CLAUDE.md routing condition 'the task will outlive this sitting' with an observable event, e.g. 'start a task needing >2 file edits (the PLAN.md row fired) and docs/STATE.md does not exist'.

## [SESSION.md | SESSION.md] line 51 (CONSTRAINT CHECK ledger keyword) vs CODE.md line 10 (C4)
ISSUE: Direct format contradiction between two files that both claim exactness. Line 47 says 'write them exactly', then line 51 gives `CONSTRAINT CHECK: <path> — none apply | matches '<text>'` — dropping C4's mandatory disposition suffix ', skipping/asking'. C4 defines `CONSTRAINT CHECK: <path> — matches '<constraint>', skipping/asking`. A model obeying SESSION.md 'exactly' prints a line that matches a constraint but states no action, violating C4; two 'exact' definitions of the same greppable artifact disagree, and the kit header forbids paraphrasing kit text.
FIX: Make line 51 a pointer only (single source, per _FORMAT.md F7): '- `CONSTRAINT CHECK:` — before the first edit of each file; exact format and procedure: docs/guardrails/CODE.md C4.'

## [VERIFY.md | VERIFY.md] line 4 (echo protocol)
ISSUE: The echo line format and the FAIL rule contradict each other for N/A. The format `V<n>: PASS|FAIL|N/A — <the command you ran + its last output line>` requires a command+output on EVERY line, including N/A, which by definition has neither; and 'An item without pasted evidence counts as FAIL' literally makes every N/A a FAIL, which combined with 'Do not write done/fixed... while any line reads FAIL' deadlocks any task where an item genuinely doesn't apply. A literal-following model (which the kit demands) hits a contradiction; a rationalizing model learns to ignore the FAIL rule — the load-bearing sentence of the whole doc. Separately, N/A requires no reason at all, making it the free escape hatch: 'V6: N/A' with nothing after it is format-legal.
FIX: Replace the format and rule with: `V<n>: PASS — <command> -> <last output line> | FAIL — <command> -> <failing line> | N/A — <one-line reason naming the absent precondition>`. Replace 'An item without pasted evidence counts as FAIL' with 'A PASS without a quoted output line counts as FAIL; an N/A without a reason counts as FAIL.'

## [VERIFY.md | VERIFY.md] line 12 (V7) vs line 25 (controlled vocabulary) vs line 36 (Todos section)
ISSUE: Status-token contradiction inside the file. V7 instructs emitting 'DONE-UNVERIFIED'; line 25's controlled vocabulary ('Controlled status vocabulary for every item you report') lists 'edited-unverified' and does NOT contain DONE-UNVERIFIED; line 36 uses a third variant, "edited, unverified"; CLAUDE.md's iron rule uses a fourth ('Edited <file>; not yet verified.'). In a kit built on exact greppable tokens, the same status has four spellings and V7 mandates a token its own file's vocabulary excludes. Grep-based compliance checks will miss variants, and a mid-tier model cannot tell which is canonical. Casing is also inconsistent within the vocab list itself (VERIFIED/NOT DONE caps vs edited-unverified/cannot-reproduce lowercase).
FIX: Pick one canonical token, e.g. EDITED-UNVERIFIED, and use it in all three places: V7 becomes '...mark every distinct deliverable VERIFIED (command) / EDITED-UNVERIFIED / NOT DONE'; line 25 vocab becomes 'VERIFIED / UNVERIFIED / EDITED-UNVERIFIED / NOT DONE / CANNOT-REPRODUCE'; line 36 becomes 'Edit landed but nothing ran = EDITED-UNVERIFIED, never complete.' (CLAUDE.md's sentence form should be aligned in the same kit edit.)

## [VERIFY.md | VERIFY.md] line 13 (V8)
ISSUE: `git diff --stat` shows only UNSTAGED changes. The doc's trigger includes 'about to run git commit', and the common flow is `git add` first — at that moment `git diff --stat` prints nothing, the model writes 'V8: PASS — git diff --stat -> (no output)', and the scope audit passes vacuously on exactly the unexplained files it exists to catch. Silent, plausible, and literally compliant — the worst kind of failure for this kit.
FIX: Change the command to `git diff --stat HEAD` (covers staged + unstaged): 'V8. Scope audit: run `git diff --stat HEAD`; give a one-line justification per changed file...'. Optionally add: 'an empty stat with edits made this task is itself a FAIL — find where the changes went.'

## [cross-doc | MIGRATE.md] M0 fresh-install branch ('do only M6a, M6d, M8 items 3 and 6, and M9') vs M9
ISSUE: M9 says 'Report the migration complete ONLY after all 8 checks pass' and requires the report to repeat the DROPPED list, UNVERIFIED-tagged facts, CONFLICTS, and Phase-1 FLAG surfaces. In fresh-install mode only M8 items 3 and 6 are run and none of those artifacts exist (no snapshot, no MIGRATION-LOG, no Phase 1). A literal-minded model can never satisfy M9's gate — the procedure is unexecutable exactly on the path M0 prescribes.
FIX: In M9, replace 'ONLY after all 8 checks pass' with 'ONLY after every check run for this mode passes (fresh install: M8 items 3 and 6 only)' and append '(fresh install: report the two check outputs and state "no original CLAUDE.md — no dispositions, conflicts, or snapshot")'.

## [cross-doc | VERIFY.md] V7 vs '## Forbidden phrases' controlled vocabulary vs '## Todos and STATE.md entries'; also CLAUDE.md iron rule 5 and DEBUG.md D1
ISSUE: The kit mandates four different literal strings for the same 'edited but unverified' status: V7 requires 'DONE-UNVERIFIED' (a token absent from the controlled vocabulary five lines below it: 'VERIFIED / UNVERIFIED / edited-unverified / NOT DONE / cannot-reproduce'); the Todos section requires "edited, unverified"; CLAUDE.md iron rule 5 requires 'Edited <file>; not yet verified.'. Also DEBUG.md D1 mandates saying 'cannot reproduce' (space) while the vocabulary lists 'cannot-reproduce' (hyphen). A greppable-compliance kit with a self-contradicting status grammar cannot be audited, and a mid-tier model gets conflicting orders at the exact reporting moment.
FIX: Pick one canonical token set and use it everywhere: change V7 'DONE-UNVERIFIED' -> 'edited-unverified'; Todos section '"edited, unverified"' -> '"edited-unverified"'; CLAUDE.md iron rule 5 '"Edited <file>; not yet verified."' -> '"edited-unverified: <file>"'; DEBUG.md D1 'Say "cannot reproduce"' -> 'Say "cannot-reproduce"'.

# MAJOR

## [CLAUDE.md | CLAUDE.md] Iron rules, lines 20, 24, 25, 26, 30, 31, 32
ISSUE: Seven iron rules RESTATE doc content instead of pointing, violating the kit's own F7 single-source contract, and the copies have already drifted: L20 restates CODE.md C1 (but omits C1's anti-loophole clauses 'a Grep snippet is not a Read', 'mandatory even for obvious one-liners'); L24 restates VERIFY.md V1; L25 restates VERIFY.md 'Forbidden phrases' but bans only 'should work' where VERIFY bans four phrases; L26 restates PLAN.md P2; L30 restates EFFICIENCY.md E14 but its token list lacks 'likely / I assume', so a model consulting only the iron rule won't trip on 'likely'; L31 restates SESSION.md's constraints update-trigger row (minus the [RECURRING] escalation); L32 restates E5+E6. Drifted twin copies mean future edits diverge further and the model can't tell which text governs.
FIX: Declare the CLAUDE.md line canonical for each pair; in the same edit, replace each doc-side restatement with a pointer ('iron rule — see CLAUDE.md') plus only the doc-specific delta, and sync the token lists now (minimum: L30 gains 'likely / I assume'; L25 gains 'should fix / likely resolves / ought to now' or points to VERIFY.md's list).

## [CLAUDE.md | CLAUDE.md] Iron rule line 23 vs docs/guardrails/CODE.md C5
ISSUE: Scope conflict between two versions of the same rule: L23 forbids calling ANY API whose signature you haven't seen this session; C5 requires pasting the signature only for 'unfamiliar or third-party API with 2+ arguments'. A model that has read CODE.md will apply C5's narrower gate and skip 1-arg APIs the iron rule covers, or vice versa be paralyzed by L23's 'any API' (which literally includes console.log). L23 is also the only iron rule with no greppable literal token (no command, path, or number), violating F9.
FIX: Replace L23 with a pointer carrying C5's literals: '- Before writing a call to a third-party or unfamiliar API with 2+ arguments: paste its real signature per docs/guardrails/CODE.md C5 (plausible is not real).'

## [CLAUDE.md | CLAUDE.md] Iron rule line 21 vs docs/guardrails/CODE.md 'You rewrote instead of edited'
ISSUE: Contradiction: L21 says 'Modify existing files with Edit, never Write' with no exception, while CODE.md's reference section explicitly authorizes rewriting a file when the user asked or more than half its lines change. A model following the authorized rewrite procedure must either use Write (breaking an iron rule, teaching it that iron rules bend) or contort a whole-file replacement through Edit.
FIX: Add the exception as a pointer: '- Modify existing files with Edit, never Write — sole exception: the rewrite procedure in docs/guardrails/CODE.md, behavior inventory first; if Edit fails twice, re-Read the region and retry Edit (memory rewrites delete real code).'

## [CLAUDE.md | CLAUDE.md] Route contract (line 17) vs iron rule line 32
ISSUE: Act-before-read loophole: L17 says 'make your next tool call Read on that doc', but L32 orders batching independent tool calls into one message. A model that batches Read(DEBUG.md) together with its attempted fix satisfies both rules literally while executing the fix before any DEBUG.md guidance can apply. Also unspecified: what to do when two rows match at once (e.g. 'return from compaction' while a test is failing) — the contract is written for exactly one matched row.
FIX: Append to line 17: 'The triggered Read rides alone in its message (other triggered Reads may batch with it) — no acting tool call in the same message. Multiple rows match? Write one TRIGGER line per row and batch those Reads.'

## [CLAUDE.md | CLAUDE.md] Route contract, '(cached)' clause (line 17)
ISSUE: The '(cached)' branch — 'obey its checklist from memory' — is the contract's gameable seam: nothing verifies the memory. A mid-tier model 80k tokens past its one read of VERIFY.md (12 items) or CODE.md (14 items + trap tables) will write '(cached)', half-remember, and silently skip items; the transcript shows a compliant TRIGGER line either way.
FIX: Bound it with a checkable artifact: append to line 17: '(cached) is valid only if you then write every checklist ID you are applying (e.g. V1–V12) in your walk; cannot list the full ID set from memory? It is not cached — Read the doc.'

## [CLAUDE.md | CLAUDE.md] Routing table row 1 (line 9)
ISSUE: The PLAN.md trigger 'start a task needing >2 file edits or touching >1 component' is a pre-task FORECAST, not an observable event — at task start the model doesn't yet know the file count, and '>1 component' is a judgment noun. This is the most rationalizable row ('probably just 2 files') on the doc that gates all planning discipline, and there is no backstop that fires when the estimate proves wrong mid-task.
FIX: Add an observable mid-task backstop to the row: '| start a task you estimate at >2 file edits or >1 component — and again the moment you are about to Edit a 3rd file with no TASK block posted | docs/guardrails/PLAN.md |'

## [CLAUDE.md | CLAUDE.md] Routing table row 5 (line 13)
ISSUE: 'are about to Read a 3rd large file' uses the judgment word 'large' — a direct violation of the kit's own F14 ('>200 lines' not 'long output') in its most-engineered block. EFFICIENCY.md already defines the number (E17: >300 lines) but the row where the trigger must actually fire has no threshold, so a mid-tier model never counts.
FIX: Row 5 -> '| are about to Read a 3rd file over 300 lines, or a search returned >50 hits | docs/guardrails/EFFICIENCY.md |' (300 matches E17), and mirror in EFFICIENCY.md line 2.

## [CLAUDE.md | CLAUDE.md] Hard stops, line 43 (taskkill/pkill stop)
ISSUE: The '-> instead:' replacement is Windows-only (`netstat -ano | findstr :PORT`) in a kit README calls portable. On macOS/Linux, findstr doesn't exist, so at the exact moment the hard stop matters the prescribed alternative fails and the model is back to improvising — the failure mode F5 exists to prevent (a prohibition whose replacement is unusable gets violated under pressure). Same Windows-only command is propagated to DEBUG.md D2 and SESSION.md.
FIX: Give both dialects, MIGRATE.md-style: '-> instead: find the PID via the port (`lsof -ti :PORT` | `netstat -ano | findstr :PORT`) then kill that PID (image-name kills take down your own harness).'

## [CLAUDE.md | CLAUDE.md] Hard stops, line 44 (delete/reset stop)
ISSUE: '-> instead: list the targets and confirm' does not say confirm WITH THE USER. Stop 1 says 'wait for approval' explicitly; the asymmetry invites a mid-tier model to self-confirm ('Confirmed — proceeding with git reset --hard') and the transcript would even look compliant. This guards irreversible data loss, so the gap matters most exactly where the stakes are highest.
FIX: Rewrite: '-> instead: paste the exact target list and wait for the user's approval in this conversation (deletion is unrecoverable).'

## [routing-sim | CLAUDE.md] Routing table, row 2 (line 10) — S6 follow-on
ISSUE: Row 2's event ('your first Edit or Write of this session') is a once-per-session event that cannot re-fire after compaction. The footer invalidates `(cached)`, but post-compaction the model reasons 'I already made my first Edit this session' — so no event exists to force CODE.md to be re-read, and its rules are silently lost for the rest of the session. The footer's 're-run the routing table' cannot rescue this because re-running row 2 literally does not match.
FIX: Reword row 2: `| are about to make your first Edit or Write since session start OR since the last compaction | docs/guardrails/CODE.md |`. Apply the same 'since the last compaction' reset to row 5's file counter.

## [routing-sim | CLAUDE.md] Routing table, row 3 (line 11) — S3 simulation
ISSUE: S3 (pytest exits 1 with traceback) fires row 3 cleanly — but the row over-fires everywhere else: 'non-zero exit' literally matches grep/findstr/rg returning no matches (exit 1) and matches the intentional red phase of TDD. A mid-tier model either Reads DEBUG.md after every empty search (token waste) or, more likely, learns the row cries wolf and starts rationalizing past it — including on real failures. Over-broad triggers decay compliance on exactly the cases they exist for.
FIX: Scope the row: `| see a failing test you expected to pass, a non-zero exit from a build/test/run command (a search returning 0 matches does not count), a traceback, or run output that contradicts what you predicted | docs/guardrails/DEBUG.md |`.

## [routing-sim | CLAUDE.md] Routing table, row 5 (line 13) — S5 simulation
ISSUE: 'a 3rd large file' — 'large' is an undefined judgment word (violates the kit's own bar #3), and the trigger requires the model to have been running a counter of 'large' files with no threshold to count against. In S5 a 2000-line file is obviously large, but was the 280-line file read earlier 'large'? A mid-tier model will not count consistently, so the row fires late or never; alternatively it can always rationalize 'that one wasn't large.'
FIX: Replace with a countable token: `| are about to Read a 3rd file after two Reads that each returned >=400 lines, or a search returned >50 hits | docs/guardrails/EFFICIENCY.md |` (>=400 is checkable from the Read tool's own output in the transcript).

## [routing-sim | CLAUDE.md] Routing table, row 6 (line 14)
ISSUE: 'the task will outlive this sitting' is a prediction, not an observable event (violates bar #2). A mid-tier model has no moment at which this trigger fires — it will only realize the task outlived the sitting after the sitting ended, which is exactly too late to have read SESSION.md and written state.
FIX: Replace with observable events: `| return from compaction or /resume, the user writes 'stop', 'pause', 'tomorrow', or 'continue later', or you complete a plan step with >5 steps remaining | docs/guardrails/SESSION.md |`.

## [routing-sim | CLAUDE.md] Routing table, row 1 (line 9) — S2 simulation
ISSUE: Two problems found in S2 ('Add CSV export to the reports page'). (1) 'start a task needing >2 file edits' requires forecasting: at start the model does not yet know the edit count, and if it discovers mid-task that scope grew, the literal 'start a task' moment has already passed — a weaker model reasons 'I'm not starting anymore' and PLAN.md never fires. (2) 'touching >1 component' — 'component' is an undefined judgment word; everything can be rationalized as one component.
FIX: Reword: `| realize — at start or mid-task — the task needs >2 file edits or edits in >1 top-level directory | docs/guardrails/PLAN.md |` ('realize' makes late discovery a fresh event; 'top-level directory' is greppable).

## [routing-sim | CLAUDE.md] Routing table, row 7 (line 15) + contract line 17 — S2 double-match
ISSUE: No rule for simultaneous multi-match, and row 7 misroutes it. In S2, if the model only realizes the task is multi-file at the moment of its first Edit, rows 1 and 2 match at once; the contract says write ONE line and make the next call Read on 'that doc' — singular, undefined for two matches. Row 7 ('are unsure which row applies -> PLAN.md') literally matches this confusion and sends the model to PLAN.md even when the genuinely-needed doc is DEBUG.md or CODE.md — a wrong-doc route. (PLAN.md line 1 also restates row 7 differently: 'no other routing row matched' vs 'unsure which row applies' — drift.)
FIX: Reword row 7: `| no row above matches but the task feels risky | docs/guardrails/PLAN.md |`, align PLAN.md line 1 to it, and append to the contract paragraph: `If 2+ rows match at the same moment, write one TRIGGER line per row and Read each matched doc, in table order, before any other tool call.`

## [routing-sim | CLAUDE.md] Routing contract, line 17 ('(cached)' clause) — adversarial cache test
ISSUE: The (cached) escape hatch is unbounded and unverifiable within a session. In a long session with no compaction, a doc read at turn 3 stays 'cached' at turn 300 with the checklist long forgotten. And 'obey its checklist from memory' leaves zero transcript evidence (violates the kit's own bar #1 — compliance must be visible): a model can write `(cached)` without genuinely recalling, or without ever having read the doc, and nothing in the moment catches it.
FIX: Make cache use checkable: `Already Read it since the last compaction? Write TRIGGER: <event> -> <doc> (cached: <the checklist item IDs, from memory>) — if you cannot write the IDs without looking, the cache is invalid: Read the doc.` Listing IDs is greppable evidence and forces an honest recall test.

## [routing-sim | CLAUDE.md] Routing contract, line 17 (final sentence) — adversarial TRIGGER-without-Read test
ISSUE: The enforcement clause is asymmetric: 'Never act on a matched trigger without one of these two lines' polices only the LINE, not the Read. A weaker model can write `TRIGGER: test failed -> docs/guardrails/DEBUG.md` (no '(cached)'), then immediately Edit — it has technically satisfied the only NEVER in the paragraph while skipping the doc. This is detectable post-hoc by an auditor, but nothing at the moment of action marks it as a violation, and satisfying the salient token is exactly how mid-tier models rationalize.
FIX: Close the loophole symmetrically: `Never act on a matched trigger without one of these two lines, and never write the non-(cached) line without the Read call in the same message — a TRIGGER line whose next tool call is not Read on that doc is itself a violation.`

## [routing-sim | CLAUDE.md] Routing table, row 4 (line 12) — S4 simulation
ISSUE: S4 fires correctly for the literal words 'Done'/'fixed', but the token list invites paraphrase evasion: 'the crash is resolved', 'all set', 'complete', 'ready to merge' match neither row 4 nor iron rule 5's list ('done/fixed/passing'), so a weaker model can make an unverified completion claim and skip VERIFY.md entirely — on the kit's highest-value doc. Timing is also the tightest in the kit: the trigger must interrupt the model between deciding to reply and streaming the reply; any wording drift and it never fires.
FIX: Extend the token list in both places and add commit/PR anchors: row 4 -> `| are about to write "done", "fixed", "works", "passing", "complete", "resolved", or "ready", or to run git commit / gh pr create | docs/guardrails/VERIFY.md |`; mirror the same seven tokens in the iron rule at line 24.

## [routing-sim | CLAUDE.md] Routing table, row 2 (line 10) — adversarial 'no row fires' test, Bash edits
ISSUE: Row 2 fires only on the Edit/Write TOOLS. A model that modifies or creates files via Bash (`cat > file`, `sed -i`, `python - <<EOF`, PowerShell `Set-Content`) never triggers CODE.md — and the iron rule at line 21 only governs the Edit-vs-Write choice, not Bash file mutation. On Windows-heavy or script-heavy sessions this is a realistic first code change that bypasses the entire CODE.md checklist.
FIX: Broaden the event: `| are about to create or modify any repo file by any means — Edit, Write, or a Bash/PowerShell command that writes files — for the first time since session start or the last compaction | docs/guardrails/CODE.md |`. Optionally add an iron rule: `Never modify files via shell redirection/sed — use Edit (shell writes bypass the diff the user reviews).`

## [budget-audit | CLAUDE.md] line 29 (iron rule: computed loop/slice bounds) + docs/guardrails/CODE.md line 15 (C7)
ISSUE: Misplaced budget layer plus a dangling pointer. C7 lists 'range endpoints' as a category with a trap-table row, but no such row exists below CODE.md's divider — the content lives only in always-on iron rule line 29. The rule fires only mid-edit, exactly when CODE.md is already loaded, so it pays ~35 always-on tokens every turn of every session (coding or not) for a moment CODE.md already owns. It is the lowest payoff-per-always-on-token iron rule of the 13.
FIX: Demote line 29 verbatim into a new CODE.md trap-table row 'Bounds & off-by-one: computed loop/slice bounds -> write a one-line trace for len=0,1,3 as a comment before the code (pattern-matched bounds are off by one)'. This simultaneously satisfies C7's currently-dangling 'range endpoints' pointer, frees an iron-rule slot (13 -> 12 of 15), and trims the always-on core.

## [MIGRATE.md | MIGRATE.md] M5, line 57 ('before writing ANY new file')
ISSUE: The STOP gate says 'WAIT for explicit approval before writing ANY new file', but by Phase 5 the procedure has already written new files three times: the snapshot (M2), MIGRATION-LOG.md (M3), and the CONFLICTS section (M4). A literal model either stalls at M3 (refusing to create the log before approval) or learns the gate's wording is not meant literally — both defeat the gate. The gate's PLACEMENT is correct (nothing destructive precedes it: overwriting CLAUDE.md and copying docs are all Phase 6+); only the wording is contradictory.
FIX: Reword M5: 'WAIT for explicit approval before Phase 6 — no kit-doc copies, no PROJECT.md/PROJECT-NOTES.md, no new CLAUDE.md. The snapshot (M2) and MIGRATION-LOG.md (M3-M4) are the only files that may exist before approval.'

## [MIGRATE.md | MIGRATE.md] M6b zone 1/zone 3, lines 68 and 70 ('everything between <!-- BEGIN KIT CORE v1.0 --> and <!-- END KIT CORE -->')
ISSUE: 'Everything between X and Y' is ambiguous about whether the marker lines themselves are included. If a model copies only the inner content, the composed CLAUDE.md has no markers — M8 item 4 (extract blocks and diff) cannot run, and UPGRADE U2's 'wholesale block swap' becomes impossible on this project forever. The whole upgrade design hangs on this one preposition.
FIX: Reword both zones: 'zone 1: the block from `<!-- BEGIN KIT CORE v1.0 -->` through `<!-- END KIT CORE -->`, BOTH marker lines included — byte-identical copy' (same for zone 3).

## [MIGRATE.md | MIGRATE.md] M6a Verify (line 65), M8 item 3 (line 83), U1 (line 94) — `git hash-object`
ISSUE: `git hash-object` applies text/CRLF filters by default (as if adding to the index). Comparing a path inside the project repo (subject to its .gitattributes/core.autocrlf) against a `<KIT>` path outside it can yield different hashes for byte-identical files. On Windows with autocrlf=true this produces false 'drift', dead-ending M6a's collision gate, failing M8 item 3, and flagging every doc as hand-edited in U1.
FIX: Use `git hash-object --no-filters docs/guardrails/<X>.md <KIT>/docs/guardrails/<X>.md` in all three places (the PowerShell Get-FileHash side is already filter-free and correct).

## [MIGRATE.md | MIGRATE.md] M1, line 21 (POSIX probe) 
ISSUE: The POSIX command `test -e .claude/settings.json; test -e .claude/settings.local.json; ls ...` prints NOTHING for the two `test -e` calls (exit codes only, and only the last survives the `;` chain), so the model literally cannot learn from the output whether the settings files exist — yet the step's whole point is a printed inventory. The PowerShell twin (`Test-Path`) prints True/False, so the pair is asymmetric.
FIX: Replace the POSIX side with output-producing probes: `for f in .claude/settings.json .claude/settings.local.json; do [ -e "$f" ] && echo "$f: EXISTS" || echo "$f: none"; done; ls .claude/commands .claude/agents .claude/skills 2>/dev/null`.

## [MIGRATE.md | MIGRATE.md] M1 line 23 ('Record every surface in the log (M3 creates it)') vs. M3 log spec (lines 34-45)
ISSUE: M1 orders recording into a log that does not exist yet, and M3's log spec defines only the disposition table (M4 adds ## CONFLICTS) — there is no ## Surfaces section and no instruction to backfill the Phase 1 inventory. M8 item 8 prints the log's headings but names no required set, so the surfaces record (including FLAG-to-user items that M9 must repeat) can silently never reach the log.
FIX: Move log creation into M1: 'Create docs/guardrails/MIGRATION-LOG.md now with a `## Surfaces` section listing each surface + decision' — and change M3 to 'Append this table to MIGRATION-LOG.md'. Add to M8 item 8: 'headings must include ## Surfaces, the disposition table, ## CONFLICTS'.

## [MIGRATE.md | MIGRATE.md] M3, line 33 ('number every non-blank, non-heading line')
ISSUE: Excluding headings from the line accounting creates an untracked loss channel: real-world CLAUDE.md files frequently carry rules AS headings ('## Always run tests before commit', '## Never touch generated/'). Such lines are never numbered, never get a disposition row, and vanish with no DROPPED record — defeating the procedure's core promise that no rule dies untraced. Row-count equality (M3 Verify, M8 item 1) still passes, so the loss is invisible.
FIX: Change to: 'number every non-blank line INCLUDING headings; a heading that is only a section label gets disposition DROPPED (decoration) or becomes a PROJECT.md anchor; a heading that states a constraint is inventoried like any rule line.'

## [MIGRATE.md | MIGRATE.md] M7, lines 76-77 (path/@-import rewrite) vs. M6b 'character-identical' and M8 item 2
ISSUE: M7 orders rewriting relative paths/@ imports in moved text, but the log's dispositions (KEPT-VERBATIM/MOVED) and M6b ('copy-pasted character-identical') leave no legal way to record the rewrite, and M8 item 2's verbatim substring spot-check can then FAIL against the original line — or worse, teach the model to cherry-pick substrings that dodge the rewritten span. Self-contradiction the model must rationalize around at exactly the anti-paraphrase checkpoint.
FIX: Add to M7: 'Record every rewrite in that row's note column as `path-rewritten: <old> -> <new>`; M8 item 2 checks such rows against the rewritten text.' 

## [MIGRATE.md | MIGRATE.md] M2 Verify (lines 30-31) vs. M8 item 7 (line 87)
ISSUE: M8 item 7 says 'Print size + hash of the untouched pre-migration snapshot', but M2's Verify recorded only filename + line count — no baseline hash exists, so printing a hash at M8 proves nothing about untouched-ness. The verify cannot actually verify what it claims.
FIX: Extend M2 Verify: 'print the snapshot filename(s), line count, AND `git hash-object --no-filters <snapshot>` | `(Get-FileHash <snapshot>).Hash`'. Extend M8 item 7: '...must equal the hash printed in M2.'

## [MIGRATE.md | MIGRATE.md] M0 fresh-install branch (line 16) vs. M9 (line 91)
ISSUE: Fresh install runs 'only M6a, M6d, M8 items 3 and 6, and M9', but M9 as written cannot be executed on that path: it gates completion on 'all 8 checks pass' (only 2 were run) and requires repeating the DROPPED list, UNVERIFIED facts, CONFLICTS, and Phase-1 FLAG surfaces — none of which exist. A literal model can never legitimately report a fresh install complete; a loose one learns M9 is negotiable.
FIX: Append to the M0 fresh-install line: 'For fresh installs, M9 reads: report complete after M8 items 3 and 6 pass (paste both); DROPPED/UNVERIFIED/CONFLICTS/FLAG sections are N/A — state that explicitly.' Also add M8 item 4 to the fresh subset, since M6d Edits ## Project after the copy and nothing else re-checks kit-zone integrity.

## [MIGRATE.md | MIGRATE.md] M0 fresh-install branch (line 16) — skipped Phase 1
ISSUE: A missing root CLAUDE.md does not mean no instruction surfaces: nested `**/CLAUDE.md`, `.claude/settings*.json` hooks injecting text, and commands/agents/skills can all exist and conflict with kit rules. The fresh path skips M1 entirely, so these are never inventoried or flagged — and M6d's 'fill ## Project with run/test commands if known' has no discovery step feeding it.
FIX: Change the fresh-install subset to 'M1 (print the inventory in chat; decisions FLAG-to-user only, no log needed), M6a, M6d, M8 items 3, 4, 6, M9-as-amended.'

## [MIGRATE.md | MIGRATE.md] UPGRADE mode, U1-U3 (lines 93-96) — missing backup, approval gate, and final verification
ISSUE: UPGRADE is entered from M0 before Phase 2, so none of the migration flow's safety rails apply: U2 rewrites CLAUDE.md's kit blocks with NO snapshot taken first, 'Show the block diff first' never says to WAIT for approval, and there is no post-swap verification at all (no re-hash of docs, no zone re-diff, no line-1 check, nothing pasted). The mode that runs on every future kit release is the least protected procedure in the file.
FIX: Add 'U0. Snapshot CLAUDE.md exactly as in M2; Verify: print filename + hash.' Append to U2: '...Show the block diff and WAIT for explicit user approval before editing.' Add 'U4. Re-run M8 items 3, 4, 5, 6 and paste each output; report complete only after all four pass.'

## [MIGRATE.md | MIGRATE.md] U1, line 94 ('a drifted file was hand-edited')
ISSUE: U1 hash-compares installed docs against the NEW `<KIT>` source, where on any real upgrade (v1.0 -> v1.1) most files differ by design — yet the parenthetical asserts every mismatch means hand-editing. The model will tell the user their files were hand-edited when they were not, and has no baseline to actually distinguish hand edits from version drift.
FIX: Reword U1: 'hashes differing from the NEW kit are EXPECTED on a version upgrade. To detect hand edits, compare the installed file against the OLD kit source if available; otherwise show the diff to the user without asserting a cause, and ask before replacing.'

## [MIGRATE.md | MIGRATE.md] U3, line 96 ('Update the version in line 1 and every replaced file')
ISSUE: Replaced docs were just copied from the new kit and already carry the new version on line 1; instructing the model to 'update the version in every replaced file' makes it Edit freshly installed kit docs — violating the file's own never-Write-kit-docs principle (M6a) and breaking hash parity with `<KIT>`, so the very next U1 flags every doc as drifted.
FIX: Reword U3: 'Update the version in CLAUDE.md line 1 only. Replaced docs already carry the new version — Verify by printing line 1 of each replaced file; never edit them.'

## [migration-dry-run | MIGRATE.md] M4, line 49 (conflict-scan trigger)
ISSUE: The scan trigger 'every original line containing must/never/always/don't/only/forbidden' has no command, no case rule, and an under-inclusive token list. Exercised misses in this legacy file: 'DO NOT delete' (line 007) matches neither don't nor never; 'NOT 3000' (line 001) unmatched; and the second REAL conflict — line 011 'just rerun it' vs DEBUG.md D5 ('Never re-run a failing command unchanged') — contains zero tokens and is never scanned, so it ships into PROJECT.md as standing advice to violate D5. Line 002's comment 'parallel = corruption' (a delta against D4/D8 self-composed test invocations that would drop --runInBand) is missed the same way.
FIX: Replace the trigger sentence with a literal command plus an activity net: 'Scan the snapshot with `grep -inE "\b(must|never|always|don'?t|do not|only|forbidden|not)\b" <snapshot>` | `Select-String -Path <snapshot> -Pattern "\b(must|never|always|don''t|do not|only|forbidden|not)\b"`; ADDITIONALLY scan every line whose subject a kit doc owns (test/build/debug/verify/commit/edit behavior) regardless of tokens — conditional imperatives ("if X, just do Y") conflict without modal words.'

## [migration-dry-run | MIGRATE.md] M3 allowed-disposition list line 44; M4(c) line 53
ISSUE: M3's verify forces a COMPLETE disposition table (row count must equal line count) before M4 runs, but the six allowed dispositions contain no pending/conflict state. Legacy line 009 ('NEVER run the integration suite locally... CI only!!') is a (c)-class conflict per M4 — yet at M3 the literal executor must already stamp it KEPT-VERBATIM or similar, and nothing says how the row is updated after the user's M5 decision, or where the line lands under each outcome. The table and the conflict resolution can end contradictory with no check catching it.
FIX: Add a seventh disposition to M3: 'CONFLICT-PENDING (points at its ## CONFLICTS entry; legal only until M5)'. Add to M4(c): 'Set the line's table row to CONFLICT-PENDING. After the user's M5 decision, rewrite the row to the decided disposition and write `resolved: <decision> <date>` in the note column; if the old rule survives, it goes KEPT-VERBATIM to `## Project`; if it constrains a kit procedure, phrase the survivor `In addition to <doc> <rule-ID>: <verbatim old rule>`.'

## [migration-dry-run | MIGRATE.md] M5, line 57
ISSUE: 'WAIT for explicit approval before writing ANY new file' is false as stated: M3 already created docs/guardrails/MIGRATION-LOG.md (and M1 says 'Record every surface in the log (M3 creates it)'). In a procedure whose entire value is literal gates, a gate that is already violated when read either stalls a literal executor or teaches it that gates are approximate — the exact rationalization habit the kit exists to suppress.
FIX: Change to: '...WAIT for explicit approval before writing ANY new file other than docs/guardrails/MIGRATION-LOG.md (created at M3, updated throughout).'

## [migration-dry-run | MIGRATE.md] M1 line 21 (surface list) and M6a line 63 (options a/c)
ISSUE: Pre-existing docs/guardrails/*.md files are not inventoried at M1 — the team's VERIFY.md release checklist is first discovered mid-M6a, AFTER the M5 STOP gate, so the user approves a migration plan that never mentioned their file is contested, then gets ambushed with a second interactive stop. Option (a) 'rename existing then install' names no rename target; option (c) 'back up then replace' names no backup convention; neither sweeps the repo for references to docs/guardrails/VERIFY.md (CI or docs pointing at the team checklist would silently start hitting the kit doc).
FIX: Add to M1: '`ls docs/guardrails/ 2>/dev/null` | `Get-ChildItem docs/guardrails -ErrorAction SilentlyContinue` — any existing file sharing a kit doc name is FLAG-to-user and MUST appear in the M5 checkpoint with the a/b/c question.' In M6a: '(a) rename to `<X>.pre-kit.md`, then grep the repo for `docs/guardrails/<X>.md` and list every reference for the user to re-point; (c) back up as `<X>.md.pre-migration-<stamp>` (M2 convention) then replace.'

## [migration-dry-run | MIGRATE.md] M8 item (3) line 83; UPGRADE U1 line 94
ISSUE: M8(3) 'Hash-compare every installed kit doc against <KIT> source' fails permanently if the user chose (b) skip for VERIFY.md at M6a: the team file's hash never matches kit source, no artifact records the skip, and the literal executor iterating kit filenames hits a mandatory FAIL it cannot resolve. U1 has the same hole — every future upgrade re-reports the team's VERIFY.md as hand-edited 'drift' and re-asks about replacing it.
FIX: In M6a, require: 'Log every collision decision under MIGRATION-LOG.md `## Kit-doc collisions`: `<file>: installed | skipped (user, <date>) | replaced (backup: <name>)`.' Rewrite M8(3): 'Hash-compare every kit doc marked installed/replaced in `## Kit-doc collisions`; print SKIPPED (user decision <date>) for skipped names instead of comparing.' Add the same lookup to U1.

## [migration-dry-run | MIGRATE.md] M6b zone 2, line 69 — vs docs/guardrails/_FORMAT.md F4 (line 7) and CLAUDE.md line 2
ISSUE: Verbatim transport of this project's caps lines ('Deploy: NEVER deploy on fridays', 'DO NOT delete', and — if the user keeps it — 'NEVER run the integration suite') pushes the composed CLAUDE.md to 6+ caps prohibition lines, while CLAUDE.md line 2 orders anyone editing the file to read _FORMAT.md first, whose F4 hard-caps caps lines at 5 per file. A literal Opus composing zone 2 faces a live contradiction, and both available 'resolutions' — downcasing NEVER or relocating the rule — are exactly the paraphrase/reachability loss MIGRATE forbids twice.
FIX: Add one sentence to M6b zone 2: '_FORMAT.md budgets F3/F4 govern the kit zones only; zone-2 project lines are exempt and keep their original casing and count.' (Mirror the scope note into _FORMAT.md F4: 'counted within kit zones; migrated `## Project` lines are exempt'.)

## [migration-dry-run | MIGRATE.md] M1, line 23 (nested CLAUDE.md handling)
ISSUE: 'Nested CLAUDE.md files are inventoried and left in place unless they conflict with kit rules' is a judgment-state condition with no executable procedure — the kit's own bar forbids exactly this. M2 snapshots only files 'you will touch' and M3/M4 operate solely on the root snapshot, so packages/worker/CLAUDE.md's lines are never numbered, never token-scanned, and the 'unless they conflict' clause can never fire. A worker-scoped rule contradicting DEBUG.md or VERIFY.md ships undetected.
FIX: Replace the clause with: 'Nested CLAUDE.md files stay in place and are never edited. Run the M4 token scan (same command) over each nested CLAUDE.md; record any overlap with kit rules in `## CONFLICTS` with the file path — nested conflicts are FLAG-to-user only, resolved by the user after M5.'

## [CODE.md | CODE.md] ## REFERENCE SWEEP, step 1 (line 27)
ISSUE: 'Grep the OLD identifier' only makes sense for renames. Most C12 triggers — signature change (name unchanged), return shape, enum member ADDED, route/flag semantics — have no 'OLD identifier'. A mid-tier model changing a function's arguments will conclude 'nothing was renamed, no OLD identifier exists, sweep N/A' and skip the sweep at exactly the moment iron rule 3 exists for.
FIX: Rewrite step 1: '1. Grep the affected symbol's name repo-wide with NO file-type filter — the OLD name if renamed, otherwise the unchanged name of the thing whose contract changed (function, key, route, enum type).'

## [CODE.md | CODE.md] C14 (line 24), under the 'After each edit:' header
ISSUE: Lifecycle misfile. C14's own text says 'paste all three greps FIRST' — it is a pre-deletion gate — but it sits under 'After each edit:'. A model walking the checklist in section order deletes the code, then reaches C14, then must revert; or treats the greps as post-hoc paperwork. This is the one clear failure of the before/while/after split.
FIX: Move C14 (ID unchanged, per _FORMAT.md F12) into the 'While editing:' block and open it with the trigger: 'C14. Before any Edit that deletes "dead" code: paste all three greps — (1) bare name repo-wide including non-code files, (2) ...' (rest unchanged).

## [CODE.md | CODE.md] C11 (line 21)
ISSUE: The entire after-edit gate is scoped 'Python file:'. There is no syntax check for JS/TS/Go/etc., and — worse — the `git diff -- <file>` unintended-change/corruption check is also trapped inside the Python-only scope, so a corrupted JS edit has no gate until VERIFY. The corruption check is language-independent and should run on every edit.
FIX: Split into two clauses: 'C11. After every edit: check `git diff -- <file>` — any change on a line you did not intend to touch is corruption: revert and redo with more context in old_string. Python file: additionally run `python -m py_compile <file>`; .js/.mjs file: `node --check <file>`; no per-file gate exists (.ts, .go): rely on the project build/typecheck at VERIFY and say so.'

## [CODE.md | CODE.md] C3 (line 9)
ISSUE: The trigger is unobservable as written: 'the target name matches more than one file or definition repo-wide?' — the model can only know this AFTER a repo-wide search, but no search is mandated. A model that doesn't already suspect a twin never fires C3 (violates the kit's own TRIGGERED bar; contrast PLAN.md's reference section, which at least presupposes a search happened).
FIX: Make the search the rule: 'C3. Twin check: Grep the target file/symbol name repo-wide before the first edit. More than one hit? List all candidates and paste the evidence for the live one (the import line or stack frame pointing at it).'

## [CODE.md | CODE.md] C7 (line 15) vs trap-table headers (lines 38-92)
ISSUE: C7's category index and the table headers don't match, breaking 'find your row': (a) the 'Familiar-API traps' table (map/parseInt, typeof null, Array holes) has NO trigger token in C7, and its traps are confidence traps — the 'unsure? probe' escape hatch never fires for them, so that table is effectively dead; (b) C7 lists 'range endpoints' but no table row exists for it; (c) C7 says 'negated conditions' while the header says 'Boolean logic' — a scanning model won't map them.
FIX: Rewrite C7's list to name the table headers verbatim: 'dates & times, epochs & units, mutation vs copy, async, floats & money, sort, division & modulo, regex & strings, familiar-API lookalikes (map/parseInt, typeof, Array holes), closures in loops, boolean logic/negation'. Drop 'range endpoints' (owned by CLAUDE.md iron rule 10 and the date-range row) or add a row for it.

## [CODE.md | CODE.md] C8 (line 16)
ISSUE: 'Grep the NEW block for each OLD token — paste zero hits' is not executable with the Grep tool: Grep is file-scoped, and in a duplicate-then-adapt the ORIGINAL block is still in the same file, so grepping for OLD tokens is guaranteed to hit it. 'Paste zero hits' can literally never be satisfied; the model must improvise, and improvised compliance is unverifiable.
FIX: Rewrite: 'C8. Duplicated-then-adapted block: list every token that had to change, Grep the file with -n for each OLD token, and confirm in one line per token that every hit's line number lies OUTSIDE the new block's range. Symmetric term appears 3+ times per line? Extract a helper instead of pasting.'

## [CODE.md | CODE.md] C5 (line 13), last sentence
ISSUE: 'Cannot produce it? Do not write the call.' is a dead-end prohibition with no ` -> instead: ` (violates the kit's own F5 and bar #4). The model NEEDS the call to finish the task; with no exit path it will rationalize ('I'm pretty sure of the signature') — the exact failure the rule targets.
FIX: Append: '-> instead: write `SIGNATURE UNVERIFIED: <fn>` and either pick an API whose signature you can paste, or stop and ask the user for the docs.'

## [CODE.md | CODE.md] C5 (line 13) vs C:/Users/Laptop/Documents/Fable optimization/CLAUDE.md iron rule 4 (line 23)
ISSUE: Same rule, two different thresholds. Iron rule 4: 'Never call an API whose signature you have not seen this session' — ANY API, unconditional (literally includes `len()` and `console.log`). C5: only 'unfamiliar or third-party API with 2+ arguments'. The iron rule as written is unworkable, so the model learns to soft-read it — which trains disobedience toward all iron rules. Single-source violation with behavioral divergence.
FIX: Align the iron rule to C5's scope: 'Never call an unfamiliar or third-party API with 2+ arguments before pasting its signature — per docs/guardrails/CODE.md C5 (plausible is not real).'

## [CODE.md | CODE.md] C4 (line 10) vs docs/guardrails/SESSION.md ledger keywords (line 51)
ISSUE: The CONSTRAINT CHECK output format is defined in two files and has ALREADY drifted: CODE.md requires `matches '<constraint>', skipping/asking`; SESSION.md's restatement is `matches '<text>'` with no ', skipping/asking'. The kit's compliance model is greppable artifacts — two formats means a compliance grep for one misses the other, and future edits will widen the gap. Violates F7 single-source.
FIX: Make CODE.md C4 the sole owner of the format. Reduce SESSION.md line 51 to a pure pointer: '`CONSTRAINT CHECK: ...` — before the first edit of each file; exact format owned by docs/guardrails/CODE.md C4.'

## [CODE.md | CODE.md] ## A test you just made pass shares literals with your fix (lines 97-98)
ISSUE: Good rule, unreachable at its trigger moment. 'A test you just made pass' happens in the DEBUG flow (D8: fix landed) or at VERIFY — the model is not re-reading CODE.md's reference section then, and neither DEBUG.md nor VERIFY.md points here. The rule will essentially never fire.
FIX: Move the section to docs/guardrails/VERIFY.md's reference section (it is a completion-gate check), or keep it and add a pointer at the end of DEBUG.md D8: 'then run the fix-shaped-to-test check in docs/guardrails/CODE.md ("A test you just made pass shares literals with your fix")'.

## [CODE.md | CODE.md] Boolean logic table, first bullet (line 90)
ISSUE: Internally incoherent at fire time: the trigger is 'negation mixed with &&/||, or 3+ clauses' but the action is 'write the truth table (max 4 rows)' — 3 clauses need 8 rows. A mid-tier model either writes 4 arbitrary rows or decides the instruction is impossible and skips it.
FIX: Replace with: 'write the truth-table rows where the expression is TRUE (up to 4 rows; more than 4 TRUE rows -> decompose into named booleans first); parenthesize every group; prefer naming the positive concept (`const isVisible = !hidden && !archived`).'

## [fact-check | CODE.md] line 54, 'Mutation vs copy' table: 'sorted()/reversed() return new'
ISSUE: reversed() does NOT return a new sequence — it returns a lazy single-use iterator over the LIVE original list. Probed (Python 3.14.4): type(reversed([1,2])).__name__ -> 'list_reverseiterator'; and src=[1,2,3]; it=reversed(src); src[0]=99; list(it) -> [3, 2, 99] (mutation after the call leaks into the 'copy'). A model told it 'returns new' will treat the result as an independent snapshot or iterate it twice; both produce wrong data silently. sorted() genuinely returns a new list (verified).
FIX: Change to: 'sorted() returns a NEW list; reversed() returns a lazy one-shot iterator over the ORIGINAL (later mutations show through; second iteration is empty) -> list(reversed(x)) or x[::-1] for a real copy.'

## [budget-audit | CODE.md] whole file; divider at line 34, trap tables lines 36-92
ISSUE: Token cost vs trigger frequency is inverted: CODE.md is the kit's heaviest doc (10,537 chars ~2,600 tokens) yet has the kit's highest-frequency trigger (first Edit of every session), making it effectively always-on for any coding session. 64 of 98 lines (~1,570 tokens, 60%) are trap tables that only matter when a C7 category fires, and roughly half the rows target languages not present in any given repo (JS+Python+Go+Java mixed).
FIX: Split the trap tables into docs/guardrails/TRAPS.md (a split-by-trigger F11 already prescribes). Rewrite C7 as the cross-doc trigger: 'C7. Touching dates/times, epoch values, regex, async, floats or money, sort, mutation-vs-copy, % or integer division, closures in loops, negated conditions, or range endpoints? Read docs/guardrails/TRAPS.md and follow your row — never guess load-bearing behavior.' Keep the REPL-probe fallback. This cuts the every-session CODE.md load from ~2,600 to ~1,050 tokens; trap-table cost is paid only in sessions that touch a trap category.

## [DEBUG.md | DEBUG.md] D6 escalation ladder (line 11)
ISSUE: Ladder levels are ambiguous and self-declared, so a model can never be held to them: (1) entry level unstated — is the first failed fix L1?; (2) 'two consecutive failures at one level' is undefined at L3/L4, which are investigation/reset actions, not fix attempts — what counts as an L3 'failure'? A model can add instrumentation forever, declare it 'succeeded' (it ran), and never reach L4/L5; (3) nothing forbids moving DOWN, so the model can oscillate L2->L1 ('back to my earlier hypothesis with a tweak') indefinitely; (4) whether an attempt is 'different fix, same hypothesis' (L1) vs 'new hypothesis' (L2) is pure self-report; (5) counter reset on level change is unstated. Also D6 merges two rules (ledger + ladder) into one ~90-word line, against F1.
FIX: Split: keep D6 as the ledger rule only, and move the ladder below the divider as a named procedure (mirroring CODE.md's REFERENCE SWEEP), with floors keyed to the checkable ledger count: 'ESCALATION LADDER (named procedure — invoked by D6). The ATTEMPT count in docs/STATE.md ## Failed attempts sets a floor that never moves down for the same bug; write the L-label into each ledger entry. ATTEMPT 3 requires a new hypothesis quoting the FULL verbatim error (L2); ATTEMPT 4 requires a pasted minimal repro or instrumentation output (L3); ATTEMPT 5 requires revert to last-known-good, naming the commit/stash (L4); ATTEMPT 6 does not exist — present the ledger to the user and stop (L5).'

## [DEBUG.md | DEBUG.md] D1 (line 6)
ISSUE: Two vacuous-satisfaction holes. (a) 'Cannot reproduce? Say "cannot reproduce" and stop' is a zero-effort exit: a model facing a laborious repro (server, seed data, UI interaction) can declare cannot-reproduce after one weak attempt and be technically compliant; no minimum procedure is required before declaring, and 'stop' has no -> instead action (report what was tried? ask for steps?). (b) D1 never requires the pasted failing output to show the REPORTED symptom — a repro command failing for an unrelated reason (missing dep, wrong cwd) satisfies the letter of D1; fix the unrelated error, command passes, bug 'fixed'.
FIX: Append two clauses to D1: 'The pasted failing output must contain the reported symptom — quote the matching line.' and replace the stop clause with: 'Cannot reproduce? -> instead: run the user's steps plus the D2 environment checks, write cannot-reproduce: <commands tried, outputs>, and ask the user for exact steps — never fix blind.'

## [DEBUG.md | DEBUG.md] D5 (line 10) vs red-flag table row 2 (line 23)
ISSUE: The flake procedure is keyed to whether the model TYPED a phrase, which makes it gameable and internally inconsistent: D5 says suspected flake -> re-run once ('flake check'), but 3x only 'if you wrote "probably flaky"'; the red-flag table says the phrase always means 3x. A model avoiding the 3x cost simply doesn't type the phrase, re-runs once, and — the biggest hole — D5 never says what a 1/1 pass licenses: one pass after one fail proves nothing, but the model will declare flake and move on.
FIX: Collapse to one procedure in D5 and make the table row a pointer: 'D5. ... Suspected flake (any wording): run the test 3x in isolation, paste all 3 outcomes; 3/3 pass -> log FLAKE: 3/3 pass isolated under docs/STATE.md ## Open items; any failure -> the bug is real.' Table row right column becomes 'Run the D5 flake procedure (3x isolated, paste all 3)'.

## [DEBUG.md | DEBUG.md] Red flags table (lines 19-28)
ISSUE: The table misses the phrases mid-tier models actually emit when stuck, so the tripwire won't fire at the moment it matters. Missing: 'let me take a different approach' (the single most common stuck-Claude phrase — masks an unlogged attempt and ladder reset), 'this failure is pre-existing / not caused by my change' (dismissal twin of 'probably unrelated' that row 1's exact-match won't catch), 'I'll add a sleep / increase the timeout' (the classic race-condition band-aid that silences the detector), and 'must be a caching issue' (rationalization D9 exists to catch).
FIX: Add four rows: | "let me take a different approach" | Write the ATTEMPT n ledger line (D6) first and name the ladder level | ; | "pre-existing / not caused by my change" | Prove it: stash your change, run on the clean tree, paste both results | ; | "add a sleep / increase the timeout" | Detector-silencing (D10): find the race or slow path first | ; | "must be a caching issue" | Run D9 now (cache-clear + marker line) and paste it before claiming |

## [DEBUG.md | DEBUG.md] D10 (line 15) vs CLAUDE.md Hard stops first bullet (line 41)
ISSUE: Single-source violation with divergent authoritative lists. D10 restates the CLAUDE.md hard stop's enumeration but the lists differ (CLAUDE.md: skips, loosened asserts, raised tolerances, `as any`, lint-disables; D10 adds deleted tests, `# type: ignore`, eslint-disable, widened catch, lowered thresholds and drops raised tolerances) — drift has already started, and a model cannot tell which list governs. D10 also uses 'see Hard stops in CLAUDE.md', the verb F10 forbids, and lacks the literal ' -> instead: ' form (F5).
FIX: Make one list canonical. Extend the CLAUDE.md hard stop to include 'widened catch, deleted tests, # type: ignore', then reduce D10 to a pointer: 'D10. About to weaken a failing check to get green (any item in CLAUDE.md ## Hard stops, first bullet)? Follow that bullet: quote the failure, propose the change, wait for approval.'

## [DEBUG.md | DEBUG.md] D2 (line 7) vs SESSION.md 'Environment beliefs expire' (lines 56-57)
ISSUE: Cross-file restatement of the same rule (F7 / kit bar 5): both give the 15-message staleness threshold and near-identical command lists (`netstat -ano | findstr :PORT`, `git branch --show-current`), and the lists already diverge (SESSION adds `pip show X` and env vars; D2 adds artifact rebuild). SESSION.md even ends its copy with '(DEBUG.md D2)' — a pointer AND a restatement, the exact pattern F7 bans.
FIX: Pick D2 as owner (it is the moment-of-action doc): merge SESSION's extra items into D2 ('server up (netstat -ano | findstr :PORT), right branch (git branch --show-current), package installed (pip show X), env var set, artifact rebuilt after the edit') and shrink SESSION.md's 'Environment beliefs expire' body to the pointer sentence only.

## [budget-audit | DEBUG.md] D6, line 11 (also D3 line 8 at 60 words)
ISSUE: D6 is 98 words on one physical line carrying ~6 distinct obligations (ledger append, append-before-retry ordering, surface-variation ban, 2-failure escalation trigger, and a 5-level ladder). The doc is 40/120 lines — 80 lines of headroom — so the packing buys no budget and costs compliance: the echo protocol cites 'D6' once for six duties, and a mid-tier model will satisfy the ledger append and silently skip the ladder. Same packing pattern: D3 (60 words), PLAN.md P6/P7/P8 (48/47/57 words).
FIX: Unpack within the existing headroom, keeping IDs stable: 'D6. After EVERY failed fix, BEFORE trying again, append to docs/STATE.md ## Failed attempts: ATTEMPT n: <what changed> -> <exact observed failure>. A next attempt differing from a logged one only in surface details is FORBIDDEN. Two consecutive failures at one level -> move UP:' followed by five indented lines L1..L5, one level per line. Split D3's WORKAROUND branch onto its own line; split P6/P7/P8 each into two lines (condition line + action line).

## [EFFICIENCY.md | EFFICIENCY.md] E3, line 8 (vs CLAUDE.md iron rule 2 and E4)
ISSUE: Two defects. (1) 'After your own Edit, never Read to confirm it applied' collides with CLAUDE.md's iron rule 'if Edit fails twice, re-Read the region and retry Edit' — the absolute 'never' gives a token-frugal model license to skip the mandated re-Read after a failed Edit. (2) 'whose scope has not changed' is undefined in E3; the definition (the event list) lives in E4, eight lines away in a different section, with no pointer. Cited or read in isolation, E3 becomes 'never re-Read, period' — the exact over-frugality the paired-floor design is supposed to prevent.
FIX: Replace E3 with: '- E3. Never re-Read a file or re-run a search unless an E4 event happened since the last one — reuse the earlier result. After a SUCCESSFUL Edit, never Read to confirm it applied (Edit fails loudly; a failed Edit follows CLAUDE.md: re-Read the region, retry Edit); confirm behavior with a command instead.'

## [EFFICIENCY.md | EFFICIENCY.md] Line 2 trigger restatement (and CLAUDE.md routing row, line 13)
ISSUE: The doc's stated trigger diverges from the routing table and violates the kit's own bar. 'or context is filling' appears only here (no routing row fires it) and is a judgment state, not an observable event — the research digest (docs/research-digest.md line 1036) had the numeric form 'context >50% used', which was dropped. 'a 3rd large file' leaves 'large' undefined in both places; a model that never knows what counts as large never counts to 3, so the doc never gets opened at the moment it matters.
FIX: Replace line 2 with: 'You are here because you are about to Read a 3rd file over 300 lines this session, or a search returned >50 hits.' and sync the CLAUDE.md routing row to 'are about to Read a 3rd file over 300 lines, or a search returned >50 hits'. If the context trigger is wanted, restore the numeric form 'context >50% used' in BOTH places, never in one.

## [EFFICIENCY.md | EFFICIENCY.md] E10, line 23
ISSUE: The primary redirect example `cmd *> build.log` is PowerShell-only syntax. Pasted into bash (README calls the kit portable; the user has a Linux build server), `*>` is not a redirect: bash glob-expands `*` against the cwd and passes every filename as an argument to cmd, then redirects — silently corrupting the command. The POSIX parenthetical covers only the extraction step, not the redirect. Secondary: the trigger 'unpredictable output' is a judgment phrase; the kit's established number for this is >200 lines (SESSION.md update-trigger table).
FIX: Replace E10 with: '- E10. Command expected to emit >200 lines (builds, installs, full suites, git log, linters): cap it (`git log -n 20`, `| Select-Object -First 50`, head_limit) or redirect and extract failures — PowerShell: `cmd *> build.log` then `Select-String -Path build.log -Pattern ''error|fail'' | Select-Object -First 30`; POSIX: `cmd > build.log 2>&1` then `grep -iE ''error|fail'' build.log | head -30`. Never print an entire log into context.'

## [EFFICIENCY.md | EFFICIENCY.md] E16, line 25
ISSUE: 'POSIX only inside the Bash tool' is factually wrong on the kit's primary deployment machine: this environment's header says 'Shell: PowerShell' and instructs PowerShell syntax in the Bash tool. A model obeying E16 literally writes POSIX into a PowerShell-backed shell and enters exactly the fail-mutate-retry loop the rule exists to prevent.
FIX: Replace E16 with: '- E16. One shell dialect per command, chosen by the shell your environment header names (the `Shell:` line) — never mix PowerShell tokens ($env:VAR, $null, Select-Object) and POSIX tokens (/dev/null, grep|head) in one command. Windows paths inside patterns: forward slashes or doubled backslashes. A quoting/syntax failure is rewritten cleanly once, never mutated-and-retried.'

## [EFFICIENCY.md | EFFICIENCY.md] E14, line 17 (vs CLAUDE.md iron rule, line 30)
ISSUE: E14 is a near-verbatim duplicate of the CLAUDE.md iron rule 'About to write probably/presumably/should be ... run the Grep or Read that answers it' — an F7 single-source violation — and the drift F7 exists to prevent has already happened: E14's tripwire list adds 'likely / I assume', the iron rule's does not. A model grepping its own draft against one list misses hedges the other list catches, and future edits will diverge further.
FIX: Make the tripwire lists grep-identical: add 'likely / I assume' to the CLAUDE.md iron rule (or trim E14 to exactly 'probably / presumably / should be'). Then record in _FORMAT.md F7 whether iron-rule/doc duplication is sanctioned (compressed always-on form + operational form) or forbidden — right now the kit does it in three places (also E5/E6, C1) while F7 bans it.

## [routing-sim | EFFICIENCY.md] Line 2 (trigger restatement) — also docs/guardrails/SESSION.md line 2; cross-check vs CLAUDE.md rows 5-6
ISSUE: Trigger drift between the router and the docs (single-source violation, bar #5). EFFICIENCY.md line 1 adds 'or context is filling' and SESSION.md line 1 adds 'or you cannot recall how the session started' — neither condition exists in the CLAUDE.md routing table, so a model reading only CLAUDE.md can never fire on them; they are dead triggers. Worse, 'context is filling' is a judgment state (bar #2 violation) with no observable token.
FIX: Delete both extra clauses from the doc line-1 restatements so they match their routing rows verbatim; if the conditions matter, add observable versions to the routing rows instead (e.g. for SESSION: 'you cannot state the user's original request in one line'). One definition, one place.

## [PLAN.md | PLAN.md] line 4 (walk instruction) + line 9-14 (P4)
ISSUE: The echo protocol says 'Walk this checklist by writing one line per item in your reply' — implying all eight P-lines are due in the reply immediately after the Read, at task start. But P3 requires Greps to have run, P5 requires a command to have run, and P4 explicitly defers itself to 'before your first Edit'. A mid-tier model resolves this tension by fabricating the lines upfront (guessed FILES in P4, an invented 'P3: none' without running the greps) — exactly the fake-compliance the kit exists to prevent. Also P4 is a 4-line block inside a 'one line per item' protocol, so the format instruction contradicts its own item.
FIX: Replace line 4 with: 'Walk items in order, running the tool calls each item needs; every `P<n>: <content, or N/A — reason>` line must appear in your transcript BEFORE your first Edit — they need not share one message. P4 is the sole multi-line item.'

## [PLAN.md | PLAN.md] line 26 ('The task turns out much bigger than you estimated')
ISSUE: The escalation trigger is 'the real change exceeds ~2x your TASK block estimate' — but the TASK block (P4) has no size-estimate field. GOAL/FILES/DONE-WHEN/CONSTRAINTS record no line count, so '2x your estimate' is uncomputable from the transcript; a mid-tier model will never fire this brake (it will rationalize 'still within what I imagined'). Only the fallback clause ('a KIND of change not planned') is checkable.
FIX: Add a fifth line to the P4 template: 'EST: ~<n> changed lines across <n> files', then rewrite line 26's opening to: 'If the real change exceeds 2x the EST line of your TASK block (either number), or needs a KIND of change not planned (schema migration, new dependency, API contract change, another service): STOP before making that change.'

## [PLAN.md | PLAN.md] line 7 (P2)
ISSUE: P2 restates the CLAUDE.md iron rule 'Treat the user's stated bug location or cause as a hypothesis; trace evidence to file:line before editing there' — a direct F7 single-source violation, and drift has already begun (P2 adds 'Reproduce the failure', the iron rule doesn't). Two divergent statements of one rule is the failure mode F7 exists to prevent.
FIX: Make P2 a pointer plus a checkable artifact instead of a restatement: 'P2. Premise (iron rule, CLAUDE.md): write `P2: premise <confirmed|disproved> at <file:line> — <repro output or trace>`.' Longer term, give the iron rules stable IDs (I1..I13) so pointers can cite by ID as F7/F12 intend.

## [PLAN.md | PLAN.md] lines 28-29 ('Your search found more than one definition of the named target')
ISSUE: This section restates CODE.md C3 (twin check) in full — list candidates, paste the import line or stack frame — violating F7, with wording already drifted (PLAN adds 'lies on the failing execution path' and 'state the choice in one line'; C3 says 'paste the evidence for the live one'). DEBUG.md line 31 does this correctly with a pointer ('check for a twin file (CODE.md C3)'); PLAN is the anomaly.
FIX: Keep the header, replace the body with a pointer: 'Common names (save, render, process, config) match multiple definitions. Run docs/guardrails/CODE.md C3 now and paste its evidence line; editing any hit before C3's evidence is pasted is forbidden.'

## [PLAN.md | PLAN.md] line 31 (reference header 'You are tempted to add an abstraction...')
ISSUE: 'You are tempted' is an internal judgment state, not an observable event — the exact thing F2 forbids and F11's example headers avoid ('Your fix didn't change the error' is observable; temptation is not). A mid-tier model mid-flight writing `class FooManager` or `npm install left-pad` never self-identifies as 'tempted', so the section — which contains the kit's only YAGNI and dependency-vetting rules — never fires.
FIX: Rewrite the header as the observable action: '## You are about to write a helper, class, interface, config option, new file, or `npm install`/`pip install` the task did not name'.

## [PLAN.md | PLAN.md] line 23 ('PLAN CHANGE' section) — dependency on docs/STATE.md
ISSUE: The PLAN CHANGE procedure mandates updating docs/STATE.md '## Next' in the same turn, and P4's CONSTRAINTS interacts with the iron rule appending to STATE.md '## Constraints' — but nothing on the PLAN.md path ensures STATE.md exists. SESSION.md ('create it at task start with EXACTLY these sections') is only read on compaction//resume or when 'the task will outlive this sitting' (itself a judgment call), so for a normal multi-file task STATE.md is never created and the model improvises a partial file with one section, breaking every later procedure that reads the skeleton (V9, SESSION update table, D6).
FIX: Add a new checklist item (new ID, per F12): 'P9. docs/STATE.md does not exist? Create it now with the exact section skeleton in docs/guardrails/SESSION.md and fill Goal/Now/Next.'

## [SESSION.md | SESSION.md] whole file structure; ledger keywords at lines 47-51 (below the '--- reference ---' divider at line 45)
ISSUE: SESSION.md is the only guardrail doc with no ID'd checklist. _FORMAT.md F11 requires 'the ID'd checklist fills the top' and F12 reserves the S1.. family; README.md line 69 greps for ANCHOR:/DETOUR(/ATTEMPT as compliance artifacts — yet the rules that produce those artifacts sit BELOW the reference divider, the zone the kit's own theory (_FORMAT.md reference section) treats as lookup material a mid-tier model reads optionally. Always-on procedures (ANCHOR before each step, DETOUR on scope departure, DECISION conformance scan) are structurally disarmed, and no SESSION rule can be cited by ID ('S3: ANCHOR: ...') as F12 demands.
FIX: Number the mandatory items S1..Sn and move them above the divider: S1 post-compaction recovery (lines 5-8), S2 STATE.md creation, S3 update-trigger table, S4 ANCHOR, S5 DETOUR/RETURNING, S6 DECISION/REVERSING, S7 CONSTRAINT CHECK pointer. Leave only 'Exact values decay', 'Environment beliefs expire', and 'Before an expensive operation' in the reference section. Add `S1:`–`S7:` to the README.md line 69 grep list.

## [SESSION.md | SESSION.md] update-trigger table, lines 33-43 (row at line 42)
ISSUE: The trigger set is missing the highest-value moment: imminent compaction. The research source (docs/research-digest.md line 1015) reads 'Before any command expected to emit >200 lines or run >2 minutes, AND BEFORE ANY /COMPACT'; SESSION.md kept only the long-command half. EFFICIENCY.md gestures at pre-compaction checkpointing but only below its own divider in a different doc. If context runs out during ordinary work (no big command involved), nothing mandates bringing STATE.md current first — the exact failure the file exists to prevent. The row's trailing clause 'route the output to a log file' also restates EFFICIENCY.md E10's procedure instead of pointing to it.
FIX: Change the line 42 row to: '| you are about to run /compact, see a context-low warning, or run a command expected to emit >200 lines or take >2 min | bring Now / Next / Failed attempts current FIRST; output routing per docs/guardrails/EFFICIENCY.md E10 |'

## [SESSION.md | SESSION.md] line 48 (ANCHOR keyword)
ISSUE: 'before each new major step' is a judgment phrase — exactly what bar #2/#3 and _FORMAT.md F2/F14 forbid, on the kit's primary anti-drift artifact. A mid-tier model rationalizes every step as not-major and never writes the line. The research source (docs/research-digest.md line 973) had the observable definition — '(starting a new todo/phase, or resuming after a detour)' — which was dropped in transcription.
FIX: Replace 'before each new major step or after any RETURNING' with 'before starting each new TodoWrite item or numbered plan step (PLAN.md P6), and after any RETURNING line'.

## [SESSION.md | SESSION.md] lines 56-57 ('Environment beliefs expire') vs DEBUG.md line 7 (D2)
ISSUE: Near-complete restatement of DEBUG.md D2 — same 15-message threshold, same `netstat -ano | findstr :PORT` and `git branch --show-current` commands — violating single-source (F7, bar #5). Drift has already begun: SESSION.md adds 'or before a compaction' and `pip show X`, which D2 lacks, so the two copies now answer 'is this belief stale?' differently.
FIX: Reduce SESSION.md to trigger + pointer: 'Server up, port bound, package installed, branch checked out, env var set — last verified >15 messages ago or before a compaction? Re-verify with one command before relying on it: commands and debugging entry per docs/guardrails/DEBUG.md D2.' Cross-file: add 'or predates a compaction' to D2 so the single owner carries the full rule.

## [VERIFY.md | VERIFY.md] lines 4 and 7 (echo protocol + V2 example)
ISSUE: The echo protocol can be pattern-completed without running anything — the exact failure it exists to prevent. Nothing ties the quoted 'last output line' to an actual tool result in the transcript; a model can type 'V2: PASS — pytest -> 12 passed in 3.2s' from imagination, and V2's verbatim example ('12 passed, 1 skipped in 3.2s') hands it the fabrication template. V1's 'produced AFTER the last edit' is asserted by the same model that skipped the run, so it self-certifies.
FIX: Append to line 4: 'Every quoted output line must appear verbatim in a tool result earlier in THIS turn — quote it, never retype it. A quoted line with no matching tool result above it is fabrication: the item is FAIL and you must run the command now.' Keep V2's example but label it: '(format example — never a line you may write without a matching tool result)'.

## [VERIFY.md | VERIFY.md] line 7 (V2)
ISSUE: No-test-suite deadlock trains N/A abuse. V2 declares '0 tests / no tests found' a FAIL of verification with no legitimate exit for a repo that genuinely has no test runner. Since FAIL blocks 'done' forever, the model's only escape is an unjustified 'V2: N/A' — teaching it that N/A is how you get past inconvenient items, which then leaks to V6/V8/V9.
FIX: Append to V2: 'Repo has no test runner? Write `V2: N/A — no test suite (Glob **/test* -> 0 hits)` — paste the Glob — and V4's behavior probe becomes mandatory (V4 may not then be N/A).'

## [VERIFY.md | VERIFY.md] line 15 (V10) vs DEBUG.md 'A previously-passing test fails after your edit' (line 34)
ISSUE: Single-source (F7 / kit bar #5) violation: V10 restates DEBUG.md's rule — quote old-expected vs new-expected plus justification, snapshot-update counts — as a full restatement, not a pointer. The two copies have already drifted slightly (DEBUG adds 'never -u as a first response'; VERIFY adds '-u counts as expectation changes'), and future edits will drift them further.
FIX: Make V10 the audit-time pointer: 'V10. You changed a test expectation or ran snapshot-update (-u) during this task? Paste the old-vs-new quote and justification required by docs/guardrails/DEBUG.md (previously-passing-test section); absent from the transcript = FAIL.' Leave the procedure itself only in DEBUG.md.

## [VERIFY.md | VERIFY.md] lines 21-25 (Forbidden phrases) vs CLAUDE.md iron rule (line 25)
ISSUE: Single-source violation with visible drift: CLAUDE.md's iron rule ('Never write "should work" — write "Verified: <command> -> <result line>" or "UNVERIFIED — to confirm, run: <command>"') and VERIFY's Forbidden-phrases section state the same rule with the same literal forms in two files. VERIFY has already extended the forbidden list ('should fix', 'likely resolves', 'ought to now') without the iron rule following, so the model gets two different scopes for the same prohibition depending on which file it last read.
FIX: Designate VERIFY.md the owner. Rewrite the CLAUDE.md iron rule as a pointer with only the two legal forms: 'Never hedge a status — only the two legal forms in docs/guardrails/VERIFY.md apply: Verified:/UNVERIFIED (hedges hide skipped runs).' Or, if iron rules must stay self-contained, shrink VERIFY's section to the delta plus 'forms defined in CLAUDE.md iron rules' — either way, exactly one file holds the canonical list.

## [VERIFY.md | VERIFY.md] lines 21, 27, 35 (reference section headers)
ISSUE: Three of four reference headers are topic nouns, violating _FORMAT.md F11 ('reference section headers are second-person situations..., never topic nouns') and the kit's own theory that topic headers never fire: 'Forbidden phrases and their mandatory replacements', 'Evidence table (use for final summaries of multi-claim work)', 'Todos and STATE.md entries'. Only the Windows CRLF header is situational. DEBUG.md and PLAN.md comply; VERIFY.md breaks its own format contract.
FIX: Rewrite as situations: '## You are about to type "should work", "should fix", or another hedge'; '## You are writing a final summary containing more than one claim'; '## You are about to mark a todo, plan step, or STATE.md entry complete'.

## [VERIFY.md | VERIFY.md] line 4 (echo protocol gate sentence)
ISSUE: The gate blocks 'done'/'fixed' and git commit while a FAIL stands, but omits 'works' — the third trigger word on line 2 and in CLAUDE.md's routing row. In a kit that assumes literal token-matching over spirit, this is a legal escape: a model with a standing FAIL line can truthfully claim 'it works now' without violating any sentence.
FIX: Change to: 'Do not write "done", "fixed", or "works", or run git commit, while any line reads FAIL.'

## [VERIFY.md | VERIFY.md] line 17 (V12)
ISSUE: The sole exemption includes 'whitespace' changes — but in indentation-significant files (.py, .yaml) whitespace IS behavior; CODE.md C11 itself treats unintended indentation change as corruption. V12 as written lets a model skip all verification on exactly the class of Python edit most likely to silently change control flow.
FIX: Rewrite the exemption: 'Sole exemption: changes touching only comments/docs, and then state "comment/doc-only change; behavior verification not applicable". Whitespace-only edits in indentation-significant files (.py, .yaml) are NOT exempt: paste `python -m py_compile <file>` (or a YAML parse) output.'

## [fact-check | VERIFY.md] line 8, V3: 'Output was piped? Print the true exit code ($LASTEXITCODE / ${PIPESTATUS[0]}).'
ISSUE: In PowerShell, $LASTEXITCODE after a pipeline is set by the LAST native command in it, not the producer — so it is NOT 'the true exit code' whenever the consumer is native. Probed (powershell -NoProfile): `python -c "import sys; sys.exit(3)" | Select-String zzz` -> $LASTEXITCODE=3 (cmdlet consumer OK), but `python -c "import sys; sys.exit(3)" | findstr zzz` -> $LASTEXITCODE=1 (findstr's no-match code; producer's 3 is lost). Worst case: a failing test run piped into a findstr that matches yields $LASTEXITCODE=0 — V3 then certifies a false PASS, the exact failure this item exists to catch. The kit itself models `| findstr` pipelines (CLAUDE.md Hard stops, DEBUG.md D2), so a Windows model will hit this. bash `${PIPESTATUS[0]}` verified correct (-> 3).
FIX: Amend V3: 'Output was piped? bash: print ${PIPESTATUS[0]}. PowerShell: $LASTEXITCODE after `a | b.exe` is b's code, not a's — never read it across a pipe into a native command (findstr etc.); instead run the producer unpiped to a log (`cmd *> run.log; $LASTEXITCODE` — the E10 pattern), then filter the log.'

## [budget-audit | _FORMAT.md] F11, line 14 ('Target cap ~120 lines')
ISSUE: The doc budget is measured only in lines, and the kit is already gaming it via line-packing: PLAN.md at 36 lines (837 words, 23.2 w/line) is heavier in tokens than SESSION.md at 60 lines (761 words, 12.7 w/line); _FORMAT itself runs 29 w/line and DEBUG 22.8. F3 explicitly bans merging rules to dodge the CLAUDE.md cap, but F11 has no equivalent guard, so the ~120-line target fails at its purpose (bounding load cost and per-rule attention) while reporting green.
FIX: Amend F11 with a token-proxy dimension and a per-line guard: 'Target cap ~120 lines AND <=1,100 words; any checklist line over 30 words splits into sub-lines under the same ID. A doc that must grow past either cap splits by trigger.' (All current docs pass the word cap; the guard exists to stop future packing.)

## [cross-doc | CLAUDE.md] ## Iron rules — six rules restate doc rules instead of pointing (violates the kit's own F7 / bar 5)
ISSUE: Six iron rules are full restatements of doc rules, with wording drift already present: (1) rule 1 vs CODE.md C1 ('imports' vs 'the import block'); (2) rule 'should work' vs VERIFY.md '## Forbidden phrases' (doc adds 'should fix / likely resolves / ought to now'); (3) rule 'probably/presumably/should be' vs EFFICIENCY E14 (doc adds 'likely / I assume' and a different action, 'Delete the sentence'); (4) constraint-capture rule vs SESSION.md update-trigger row 2; (5) batching rule vs EFFICIENCY E5+E6; (6) premise-as-hypothesis rule vs PLAN P2. _FORMAT F7 says a rule lives in exactly one file and cross-references are pointers only — the kit's core violates its own contract, and the drifted word lists mean the two copies trigger on different phrases.
FIX: Amend F7 with an explicit two-tier exception: 'An iron rule may compress a doc rule; the doc line must cite the iron rule, and any trigger phrase list or threshold must be byte-identical in both.' Then sync the drifted lists: iron rule 10 gains 'likely / I assume'; VERIFY's forbidden-phrase list is referenced from iron rule 6 by pointer ('...per docs/guardrails/VERIFY.md Forbidden phrases') instead of restating two of its four phrases.

## [cross-doc | CLAUDE.md] Routing table row 5 ('are about to Read a 3rd large file'); mirrored in docs/guardrails/EFFICIENCY.md line 2
ISSUE: 'large' is a judgment word with no number anywhere in the kit — EFFICIENCY defines 250/300/400-line thresholds but never defines 'large'. The routing table is the firing mechanism; a mid-tier model rationalizes 'not that large' and EFFICIENCY.md never opens. This violates the kit's own bar 3 / F14 in its most engineered block.
FIX: Routing row: 'are about to Read a 3rd file >300 lines, or a search returned >50 hits'. EFFICIENCY line 2: 'You are here because you are about to Read a 3rd file >300 lines or a search returned >50 hits.'

## [cross-doc | CLAUDE.md] KIT FOOTER post-compaction line vs docs/guardrails/SESSION.md '## Returned from compaction' block
ISSUE: The post-compaction procedure is defined in two places with different step lists: footer = Read docs/STATE.md + re-run routing table; SESSION = Read docs/STATE.md + git status/git diff --stat + restate Goal/Next. The sentence 'docs read before compaction no longer count as read' appears near-verbatim in both. Following both in order also forces a second Read of docs/STATE.md that EFFICIENCY E3 forbids. Single-source violation with live drift.
FIX: Footer becomes pure re-arm + pointer: 'After compaction or /resume: Read docs/guardrails/SESSION.md and follow its first block; `(cached)` is invalid until you Read the doc again.' SESSION keeps sole ownership of the steps and of 'Docs read before compaction no longer count as read.'

## [cross-doc | MIGRATE.md] M5 ('WAIT for explicit approval before writing ANY new file') vs M3 ('Create docs/guardrails/MIGRATION-LOG.md') and M2 snapshot; README.md 'Install — project with an existing CLAUDE.md' ('stops for your approval before writing anything')
ISSUE: M3 (Phase 3) creates MIGRATION-LOG.md and M2 writes a snapshot, both before the Phase 5 gate that forbids writing 'ANY new file' before approval. A rule-follower that reads the whole procedure first (as instructed) sees a contradiction at a STOP gate; README's claim 'stops for your approval before writing anything' is false as written.
FIX: M5: change to 'WAIT for explicit approval before writing any file other than the M2 snapshot and docs/guardrails/MIGRATION-LOG.md'. README: change 'it stops for your approval before writing anything' to 'it stops for your approval before installing kit docs or composing the new CLAUDE.md'.

## [cross-doc | MIGRATE.md] UPGRADE mode U1
ISSUE: U1 says 'Hash-compare each installed docs/guardrails/*.md against <KIT> source', but after a migration that directory legitimately contains project-owned files with no kit source: MIGRATION-LOG.md (M3), PROJECT.md (M6c), PROJECT-NOTES.md (M6c). The compare either errors or reports phantom 'drift' on files that were never kit files, and the model is told to ask the user about replacing them.
FIX: U1: change to 'Hash-compare each installed docs/guardrails file that also exists in <KIT>/docs/guardrails/ (skip MIGRATION-LOG.md, PROJECT.md, PROJECT-NOTES.md — project-owned, never kit-sourced); print pairs; report drift...'.

## [cross-doc | MIGRATE.md] M6b 'Compose the new CLAUDE.md'
ISSUE: M6b names no tool (violating F10, 'kit verbs are tool names') and, whenever the kit core is loaded in the executing session (upgrade mode, or migrating a second project from a kit-installed one), it directly conflicts with iron rule 2 'Modify existing files with Edit, never Write' — replacing the project CLAUDE.md wholesale requires Write, and no exemption is stated. A rule-following model stalls or improvises at the most delicate step.
FIX: M6b first line: 'Write the new CLAUDE.md (Write is correct here and supersedes the kit iron rule "Edit, never Write" for this one step — the M2 snapshot is the backup) with exactly this structure...'

## [cross-doc | SESSION.md] '## Session ledger keywords', CONSTRAINT CHECK line, vs CODE.md C4
ISSUE: The CONSTRAINT CHECK grammar differs between its two definitions: CODE.md C4 mandates `CONSTRAINT CHECK: <path> — matches '<constraint>', skipping/asking`; SESSION.md gives `CONSTRAINT CHECK: <path> — none apply | matches '<text>'`, dropping the mandatory ', skipping/asking' action tail and renaming the placeholder. Two sources with different formats is both a grammar drift and a single-source (F7) violation; a model following SESSION's form matches a constraint and takes no prescribed action.
FIX: Make SESSION's line a pointer only: '`CONSTRAINT CHECK:` — before the first edit of each file; exact format owned by docs/guardrails/CODE.md C4.' (Delete the divergent format string.)

## [cross-doc | SESSION.md] whole file — no S-series checklist IDs; _FORMAT.md F12 and F11
ISSUE: _FORMAT F12 declares 'S1..' as a stable ID family and F11 requires 'the ID'd checklist fills the top', but SESSION.md contains zero S-IDs — its mandatory items (post-compaction steps, template creation, the 8 update-trigger rows) cannot be cited by ID, so SESSION compliance is the one doc invisible to the ID-grep audit README prescribes.
FIX: Assign IDs without restructuring: S1 = the 3-step post-compaction block, S2 = create docs/STATE.md with the exact template, S3 = the update-trigger table (rows S3a-S3h or cite 'S3: <row>'), and add `S1:`-`S3:` to README's audit-token list.

## [cross-doc | _FORMAT.md] F5 vs every NEVER line outside CLAUDE.md '## Hard stops'
ISSUE: F5 requires every NEVER/Don't line to contain the literal ` -> instead: `. Only the 4 hard stops comply. Dozens of NEVER lines across the docs use an em-dash or bare '->' (e.g. CODE.md trap tables 'Never add 86400s... -> use addDays', DEBUG D5 'Never re-run a failing command unchanged. Before any re-run...', EFFICIENCY E12, VERIFY 'Never write "should work"'). The contract as written is pervasively violated by the kit itself, so a grep for '-> instead:' — the kit's own audit mechanism — reports near-total non-compliance and the contract is unenforceable.
FIX: Amend F5 to match reality and stay auditable: 'F5. Every NEVER/Don't line carries its replacement action on the same line after `->` or an em-dash; the literal ` -> instead: ` form is mandatory only for the CAPS hard-stop lines in CLAUDE.md.'

# MINOR

## [CLAUDE.md | CLAUDE.md] Hard stops, line 42 (git push stop)
ISSUE: The literal condition 'unless the user wrote "push" in this conversation' is satisfied by the user writing 'don't push yet' — the token appears, the rule as written permits the push. Literal-token triggers are the kit's strength, but this one tests for the wrong thing (word presence, not instruction).
FIX: Require a checkable quote: '- NEVER run `git push` unless the user asked for a push in this conversation — quote their words beside the command -> instead: commit locally and report (publication is irreversible).'

## [CLAUDE.md | CLAUDE.md] Routing table row 4 (line 12) vs iron rule line 24
ISSUE: Token-list drift for the same event: row 4 fires on 'done', 'fixed', 'works'; L24 governs 'done/fixed/passing'. A model writing 'passing' or 'complete' matches the iron rule but not the routing row, so VERIFY.md never opens; synonyms ('resolved', 'all set') dodge both.
FIX: Unify one token list and use it in both places: row 4 -> '| are about to write "done", "fixed", "works", "passing", or "complete", or run git commit | docs/guardrails/VERIFY.md |'; L24 -> 'Claim done/fixed/works/passing/complete only beside...'. Mirror in VERIFY.md line 2.

## [CLAUDE.md | CLAUDE.md] Iron rules lines 24-25 vs docs/guardrails/VERIFY.md controlled vocabulary
ISSUE: Three competing formats for the same unverified state: L24 mandates 'Edited <file>; not yet verified.', L25 mandates 'UNVERIFIED — to confirm, run: <command>', and VERIFY.md's controlled vocabulary says 'edited-unverified'. An auditor grepping a transcript has no single marker, and a model must guess which form is legal at the moment of writing.
FIX: Standardize on L25's form everywhere: L24 -> '...otherwise write "UNVERIFIED — to confirm, run: <command>"'; align VERIFY.md's vocabulary line; add the marker to README.md's audit-grep list.

## [CLAUDE.md | CLAUDE.md] Routing table row 6 (line 14)
ISSUE: 'the task will outlive this sitting' is a prediction/judgment state (no number, nothing observable), violating F2/F14; a mid-tier model will never conclude mid-flow that a task 'will outlive the sitting'. SESSION.md line 2 also adds a third trigger ('you cannot recall how the session started') that exists only inside the doc it is supposed to route to — it can never fire.
FIX: Row 6 -> '| return from compaction or /resume, or the task is still unfinished after 30+ tool calls | docs/guardrails/SESSION.md |', and make SESSION.md line 2 restate exactly that.

## [CLAUDE.md | CLAUDE.md] Routing table row 7 (line 15) vs docs/guardrails/PLAN.md line 2
ISSUE: The fallback row 'are unsure which row applies' is a judgment-state trigger (the kit's own F2 forbids these), and PLAN.md line 2 restates it as a different condition — '(or no other routing row matched)' — which read literally would route EVERY trivial action to PLAN.md. Same drift pattern in EFFICIENCY.md line 2, which adds 'or context is filling' (a judgment state with no routing row, so it can never fire).
FIX: Make row 7 and PLAN.md line 2 identical, e.g. row -> '| a task is starting and no row above matched | docs/guardrails/PLAN.md |' and PLAN.md line 2 -> '...or a task started and no other routing row matched'. Delete or route 'context is filling' in EFFICIENCY.md line 2.

## [CLAUDE.md | CLAUDE.md] Footer line 46 vs docs/guardrails/SESSION.md recovery block
ISSUE: The footer's 'Read docs/STATE.md, then re-run the routing table' duplicates SESSION.md's 'Do this FIRST' step 1 (which also adds git status + goal restatement the footer omits). Post-compaction the model is told to Read STATE.md by the footer, then row 6 routes it to SESSION.md whose step 1 orders the same Read again — a duplicated instruction with a drifted procedure (F7), and EFFICIENCY E3 forbids the second read.
FIX: Single-source recovery in SESSION.md; footer becomes only the re-arm + cache rule: 'After compaction or /resume: the routing row "return from compaction" has fired — write its TRIGGER line and Read docs/guardrails/SESSION.md before any file-modifying call; docs read before compaction no longer count as read — `(cached)` is invalid until you Read the doc again.'

## [CLAUDE.md | CLAUDE.md] Iron rule line 31
ISSUE: Appending constraints 'to docs/STATE.md under ## Constraints' assumes the file and section exist, but the template that creates them lives in SESSION.md, which is only routed on compaction//resume/long tasks. In a short task where the user states a constraint, the model either fails the append or invents an ad-hoc STATE.md missing the mandated sections, which later conflicts with SESSION.md's 'create it with EXACTLY these sections'.
FIX: Append a creation pointer: '...append it verbatim to docs/STATE.md under `## Constraints` — file missing? Create it per docs/guardrails/SESSION.md first (unwritten constraints decay within 50 turns).'

## [CLAUDE.md | CLAUDE.md] Iron rule line 29 vs docs/guardrails/CODE.md C7
ISSUE: Two different procedures own the same event: C7 routes 'range endpoints' to the trap tables, but no range-endpoints trap row exists — the de-facto rule (trace len=0,1,3) lives as CLAUDE.md iron rule L29 instead. A model at C7 with a slice-bounds edit finds no row and falls back to a REPL probe, while the always-on rule demands a trace comment; which action satisfies compliance is ambiguous, and a narrow language trap is spending an always-loaded iron slot the trap tables were built to relieve.
FIX: Move L29 verbatim into CODE.md's trap tables as the 'Range endpoints' row C7 already advertises, freeing an iron-rule slot; C7 remains the single trigger.

## [CLAUDE.md | CLAUDE.md] Layout (iron rules, lines 19-33) vs docs/guardrails/_FORMAT.md F8
ISSUE: F8 mandates 'nothing load-bearing sits in the middle third' of CLAUDE.md, but on a fresh install (47 lines) the iron rules — the most load-bearing block — occupy exactly the middle third (lines ~19-33). The shipped file cannot satisfy its own authoring contract, so the first person to edit the kit hits an unresolvable rule.
FIX: Fix F8's wording to describe the real invariant: 'F8. CLAUDE.md layout: routing table first, iron rules immediately after it, hard stops + the post-compaction re-arm line last; ## Project is the only block between iron rules and the footer.'

## [routing-sim | CLAUDE.md] Routing table, rows 3 and 5 (lines 11, 13) — adversarial wrong-doc test
ISSUE: Overlap: a search that returns 400 hits is simultaneously '>50 hits' (row 5) and 'output that surprised you' (row 3). A weaker model can plausibly route a search-volume problem to DEBUG.md. The row-3 scoping fix ('run output that contradicts what you predicted') resolves this; noting it separately so the overlap is checked after that fix lands.
FIX: Covered by the row 3 rewording (restrict 'surprised' to build/test/run output); verify no other output-shaped event matches both rows afterward.

## [routing-sim | CLAUDE.md] Routing table header, line 6 vs contract line 17
ISSUE: The header says 'Read the doc BEFORE your next tool call', but reading the doc IS a tool call, so the header is literally unsatisfiable; the contract's version ('make your next tool call Read on that doc') is the correct one. A pedantic mid-tier model can notice the impossibility and treat the whole routing header as loose guidance.
FIX: Change the header to match the contract: `## Routing — the moment X happens, your next tool call is Read on the doc`.

## [budget-audit | CLAUDE.md] line 17 (routing protocol paragraph under the table)
ISSUE: 52 words, 3 sentences — the longest always-on line in the file (~65 tokens every turn) and over F1's <=20-word target by 2.5x. A tighter phrasing loses nothing.
FIX: Replace with: 'Row matched: write `TRIGGER: <event> -> <doc>`; your next tool call is Read on that doc. Already Read since last compaction: write `TRIGGER: <event> -> <doc> (cached)` and obey its checklist from memory. Acting on a match without one of these lines is a violation.' (~44 words, same two legal artifacts, same prohibition.)

## [MIGRATE.md | MIGRATE.md] M8 item 2, line 82
ISSUE: Two gaps: (a) 'an exact >=15-character substring' cannot be run for original lines shorter than 15 characters (e.g. 'Port: 5173'); (b) '10 random rows' has no selection procedure, so a model under pressure will pick the rows it knows will pass.
FIX: Reword: 'for 10 rows selected as every Nth row by row number (N = rowcount/10, minimum every row if <=10), grep the destination for an exact substring of >=15 characters, or the entire line if shorter.'

## [MIGRATE.md | MIGRATE.md] M2, lines 29-30
ISSUE: (a) POSIX Verify gives bare `wc -l` (reads stdin — missing the filename operand, and disagrees with `(Get-Content).Count` by one when the file lacks a trailing newline); (b) 'If the repo is clean, also commit the snapshot' is ambiguous — the snapshot is untracked so a 'clean' check is confusing, and if the repo is dirty no committed baseline exists at all, with no stated fallback.
FIX: (a) Write `wc -l <snapshot>`. (b) Reword: 'If `git status --porcelain` (excluding the snapshot) is empty, `git add <snapshot> && git commit -m "[chore] pre-migration snapshot"`; if dirty, note SNAPSHOT-UNCOMMITTED in the log and continue.'

## [MIGRATE.md | MIGRATE.md] U1, line 94 ('show `diff` | `Compare-Object` output')
ISSUE: The PowerShell half is not a runnable command: bare `Compare-Object` on two paths compares the path strings, not file contents. The POSIX half (`diff`) is fine.
FIX: Give the exact form: '`diff docs/guardrails/<X>.md <KIT>/docs/guardrails/<X>.md` | `Compare-Object (Get-Content docs/guardrails/<X>.md) (Get-Content <KIT>/docs/guardrails/<X>.md)`'.

## [MIGRATE.md | MIGRATE.md] M8 item 4, line 84
ISSUE: 'Extract the KIT CORE and KIT FOOTER blocks... and diff each' names no command, in a file whose own standard is literal greppable commands; the model improvises the extraction, which is exactly where marker-boundary mistakes creep in.
FIX: Add the pair: `sed -n '/BEGIN KIT CORE/,/END KIT CORE/p' CLAUDE.md > /tmp/proj-core.md` (repeat for FOOTER and for `<KIT>/CLAUDE.md`), then `diff` | PowerShell: `Select-String` range extraction or `Compare-Object` on the same slices.

## [MIGRATE.md | MIGRATE.md] M6b structure (lines 66-70) vs. CLAUDE.md line 2
ISSUE: The kit template CLAUDE.md has line 2 (`<!-- Editing this file? Read docs/guardrails/_FORMAT.md first. Never paraphrase kit text. -->`) OUTSIDE the kit markers, and M6b's three-zone compose spec never mentions it — so every migrated project silently loses the anti-paraphrase pointer that protects future CLAUDE.md edits.
FIX: Either add to M6b: 'line 2: copy CLAUDE.md's editing-warning comment verbatim', or (better, a template fix) move that comment inside the BEGIN KIT CORE markers so zone-1 copying and U2 block swaps carry it automatically.

## [MIGRATE.md | MIGRATE.md] M0, lines 14-16
ISSUE: Idempotency checks only the CLAUDE.md sentinel. An aborted prior run (snapshot + MIGRATION-LOG.md exist, CLAUDE.md not yet composed) passes M0, then M3 'Create docs/guardrails/MIGRATION-LOG.md' clobbers the previous run's log — the one artifact that records what happened to every original line.
FIX: Add to M0: 'Also run `test -e docs/guardrails/MIGRATION-LOG.md` | `Test-Path docs/guardrails/MIGRATION-LOG.md`. If it exists without the CLAUDE.md sentinel: a prior run aborted — print its section headings and ask the user whether to resume from it or archive it before starting over.'

## [MIGRATE.md | MIGRATE.md] M6c (line 72) / M3 (line 45) vs. docs/guardrails/_FORMAT.md line 2 and F1/F11
ISSUE: MIGRATE creates PROJECT.md and PROJECT-NOTES.md inside docs/guardrails/, where _FORMAT.md's trigger ('about to edit... any docs/guardrails file') imposes one-line-imperative rules, a trigger line 1, and the ~120-line doc shape — obligations that verbatim-transported project prose cannot and must not meet. A model that obeys _FORMAT's trigger is pushed toward reformatting (i.e., paraphrasing) the very lines MIGRATE forbids touching.
FIX: Add one line to M6c: 'PROJECT.md and PROJECT-NOTES.md are project-authored archives EXEMPT from docs/guardrails/_FORMAT.md doc-shape rules — never reformat their transported content' (and consider stating the exemption in _FORMAT.md line 2 as well).

## [migration-dry-run | MIGRATE.md] M1, line 22 (hooks bullet)
ISSUE: 'Read any hooks configured in those settings files and note what text they inject each session' fits SessionStart-style injection hooks only. This project's hook is PostToolUse — it injects nothing per-session, so a literal executor records 'N/A' and the hook's enforcement behavior (which could be the very lint gate that legacy line 004's disposition depends on) goes un-inventoried.
FIX: Rewrite the bullet: 'Read every hook in those settings files; for each, note its event (SessionStart/PostToolUse/etc.), its command, and what it injects OR enforces.'

## [migration-dry-run | MIGRATE.md] M3 example table row 005, line 42
ISSUE: Example row 005 ('TODO: retire old auth code ... UNSORTED -> PROJECT-NOTES.md') collides by subject with this project's legacy line 007 — 'The old auth code in src/legacy-auth/ is dead but DO NOT delete it until the mobile app v3 ships' — which is a hard rule, not a note. A pattern-matching mid-tier model primed by 'old auth code -> UNSORTED' routes the DO-NOT-DELETE constraint into a never-loaded notes file; the next cleanup session deletes src/legacy-auth/.
FIX: Make every example row obviously synthetic, e.g. `| 005 | TODO: revisit widget-cache sharding someday | UNSORTED | docs/guardrails/PROJECT-NOTES.md | not a rule; keep verbatim |` (belt-and-braces with the 'example rows are SHAPE only' sentence from the row-003 fix).

## [migration-dry-run | MIGRATE.md] M8 item (4), line 84
ISSUE: 'Extract the KIT CORE and KIT FOOTER blocks ... and diff each' names no command — the only M8 item without one, violating the kit's own F9/F10 bar (literal greppable command in every rule). A mid-tier model may 'diff' by eyeball and assert equality.
FIX: Give the command: `sed -n '/BEGIN KIT CORE/,/END KIT CORE/p' CLAUDE.md > /tmp/core.new; sed -n '/BEGIN KIT CORE/,/END KIT CORE/p' <KIT>/CLAUDE.md > /tmp/core.kit; diff /tmp/core.new /tmp/core.kit` (repeat for FOOTER) | PowerShell: extract with `Select-String -Context` or index slicing, then `Compare-Object`. Print the empty diff.

## [SESSION.md | README.md] line 69 (compliance-audit grep list)
ISSUE: The greppable-artifact list omits `DECISION:` (and `REVERSING DECISION`), which SESSION.md line 50 defines as ledger keywords that 'are greppable compliance artifacts'. An auditor following the README will never check DECISION compliance. (ANCHOR:, DETOUR(, CONSTRAINT CHECK:, ATTEMPT all match SESSION.md correctly.)
FIX: Add `DECISION:` to the README.md line 69 list: '...`ATTEMPT `, `ANCHOR:`, `DETOUR(`, `DECISION:`, `CONSTRAINT CHECK:`, ...'.

## [budget-audit | README.md] line 13 ('Deliberately small (~55 lines)') and line 53 ('Only ~55 lines are always loaded')
ISSUE: The kit misreports its own headline measurement: actual CLAUDE.md is 47 lines total, 40 within the kit markers. '~55' is the budget cap, not the measurement — a 15-20% overstatement in a kit whose whole pitch is numbers-not-judgment.
FIX: Line 13: 'Deliberately small — 40 kit lines as shipped (hard cap 55).' Line 53: 'Only ~40 kit lines (47 with the Project stub) are always loaded.'

## [CODE.md | CODE.md] ## You rewrote instead of edited (lines 94-95)
ISSUE: No checklist item routes to this section, so it is only found by chance at the moment of rewriting; and 'rewriting a file is allowed if... more than half its lines change' reads as permission to use Write on an existing file, brushing against CLAUDE.md iron rule 2 ('Modify existing files with Edit, never Write').
FIX: Add a one-line hook in 'While editing:': 'C15. Replacing more than half of a function or file? Follow "You rewrote instead of edited" below the divider first.' In the section itself add '(still via Edit — never Write on an existing file, per CLAUDE.md iron rule 2)'.

## [CODE.md | CODE.md] C2 (line 8)
ISSUE: 'a lockfile' is not a greppable literal (violates the kit's own F9); every other token in the rule is a literal path fragment.
FIX: Replace 'a lockfile' with the literals: 'package-lock.json, yarn.lock, pnpm-lock.yaml, Cargo.lock, poetry.lock, uv.lock'.

## [CODE.md | CODE.md] C10 (line 18)
ISSUE: 'New import: confirm the target exists' fires on stdlib imports (`import os`, `import json`) where Glob/Grep of the repo can never hit — the model either wastes calls or learns to skip C10, eroding obedience.
FIX: Scope it: 'C10. New PROJECT-LOCAL import (relative path or repo package): confirm the target exists — Glob the module path or Grep the export (`def <sym>|class <sym>|export .*<sym>`) — and paste the hit. Third-party imports are covered by C6.'

## [CODE.md | CODE.md] ## REFERENCE SWEEP header and steps (line 26)
ISSUE: Two small compliance-mechanics gaps: (a) the steps are numbered 1-5 but carry no stable citable IDs, yet the kit's protocol is 'cite the ID with one line of evidence'; (b) 'invoked by C12 and the iron rules' is a vague pointer — F10 requires exact path/ID pointers.
FIX: Rename steps RS1-RS5 (stable per F12) and change the header parenthetical to '(named procedure — invoked by C12 and CLAUDE.md iron rule 3)'.

## [CODE.md | CODE.md] C9 (line 17), last sentence
ISSUE: 'Never replace_all a string shorter than a full identifier' is ambiguous (shorter than WHICH identifier?) and the Never has no ` -> instead: ` replacement (F5).
FIX: Rewrite: 'Never replace_all a string that is not a complete identifier, or one that can occur inside another word -> instead: run one Edit per occurrence with enough surrounding context to be unique.'

## [CODE.md | CODE.md] C4 (line 10)
ISSUE: No branch for the common fresh-session case where docs/STATE.md does not exist yet — the model cannot truthfully print either legal form and will improvise or skip.
FIX: Add a third legal output: 'or `CONSTRAINT CHECK: <path> — no docs/STATE.md yet` (then create it per docs/guardrails/SESSION.md if the task will outlast this turn).'

## [CODE.md | CODE.md] Async table, first bullet (line 59)
ISSUE: 'grep the file for each async function name not preceded by await/return/void' is not a single executable grep — the Grep tool's default regex engine has no lookbehind. As written the model may burn attempts on an impossible pattern.
FIX: Reword to the two-step it actually means: 'Grep the file for each async function name; for every hit not preceded by await/return/void on that line, justify it in one line.'

## [fact-check | CODE.md] line 73, 'Division & modulo' table, first row
ISSUE: The three minus signs in '(−1 % 5 = 4)' and '(−1 % 5 = −1)' are Unicode U+2212, not ASCII hyphen-minus (confirmed by grepping the file for the U+2212 character — line 73 is the only guardrail hit). C7 and this table explicitly tell the model to run one-line REPL probes; a model that copies the token verbatim gets `python -c "print(−1 % 5)"` -> SyntaxError: invalid character '−' (U+2212), a wasted failure cycle in the doc that is supposed to prevent them. Violates the kit's own 'literal greppable tokens' bar (a grep for '-1 % 5' misses it).
FIX: Replace both U+2212 characters on line 73 with ASCII '-': 'Python // and % FLOOR (-1 % 5 = 4); JS/Java/C/Go/Rust % TRUNCATES (-1 % 5 = -1).'

## [fact-check | CODE.md] line 79, 'Regex & strings' table: 'use [...str] for user-visible truncation'
ISSUE: [...str] splits by code point, not grapheme cluster, so it still cuts user-visible glyphs. Probed (node v24.14.1): [...'\u{1F44D}'].length -> 1 (thumbs-up OK), but [...'\u{1F468}‍\u{1F469}‍\u{1F467}'].length -> 5 (one visible family emoji = 5 code points); slicing a spread at any of those boundaries renders broken partial emoji. The claim ''👍'.length === 2' itself is correct (verified).
FIX: Append a caveat: 'use [...str] (code points; still splits ZWJ emoji/combining marks — for true grapheme-safe truncation use Intl.Segmenter).'

## [fact-check | CODE.md] lines 53 and 70 (toSorted/toReversed/toSpliced), line 55 (structuredClone)
ISSUE: The recommended replacements are runtime-version-dependent and the table never says so: toSorted/toReversed/toSpliced are ES2023 (Node >=20, Safari >=16.4; verified present on local Node 24.14.1), structuredClone is Node >=17. In a codebase targeting older browsers or a pinned Node 18, the kit's mandated fix itself throws 'x.toSorted is not a function' — and C6 ('write v<N> idioms') only covers library versions, not the runtime.
FIX: Add one clause at line 53: 'Copying: toSorted, toReversed, toSpliced (ES2023 — Node 20+/2023 browsers; older targets: [...x].sort()), slice, concat, spread' and at line 55: '-> structuredClone (Node 17+).'

## [DEBUG.md | DEBUG.md] 'A previously-passing test fails after your edit' (lines 33-34)
ISSUE: Restates VERIFY.md V10 ('quote old-expected vs new-expected with the justification') across files — single-source violation and drift risk. The intent-gate and the snapshot-update prohibition are unique and should stay; the expectation-recording clause is V10's.
FIX: Replace 'Only if intended: edit the expectation and quote old-expected vs new-expected with the justification.' with 'Only if intended: edit the expectation and record it per docs/guardrails/VERIFY.md V10.'

## [DEBUG.md | DEBUG.md] D3 (line 8), final clause
ISSUE: 'plus a grep for every other place the same bad state flows' has no paste verb and no pattern spec — the doc's only check-verb-style clause; a model will 'do a grep' mentally and move on.
FIX: Replace with: 'plus Grep the originating symbol/value repo-wide and paste the hit list of every other place the same bad state flows.'

## [DEBUG.md | DEBUG.md] 'The failure only happens in CI' (lines 39-40)
ISSUE: The only section with no greppable literal token (F9): 'Grep the CI config' names no path, so a model won't know where to look.
FIX: Replace 'Grep the CI config' with 'Grep .github/workflows/*.yml (or .gitlab-ci.yml, azure-pipelines.yml, Jenkinsfile)'.

## [DEBUG.md | DEBUG.md] Red flags table, 'should work now' row (line 24)
ISSUE: The remedy ('Run it; paste the last output line') diverges from the kit's two-legal-forms rule for this exact phrase (CLAUDE.md iron rule + VERIFY.md forbidden phrases) — a third variant of the same rule in a third file.
FIX: Right column -> 'Run it; write Verified: <command> -> <result line> (the CLAUDE.md legal form)'.

## [DEBUG.md | DEBUG.md] D9 (line 14)
ISSUE: `pytest --cache-clear` is miscast as a stale-code remedy — it only clears pytest's test-selection cache and cannot make edited code get executed; the decisive step (marker line / printed path + mtime) is listed second, so a model may run --cache-clear and skip the proof.
FIX: Reorder to make the code-identity proof primary and demote cache flags: 'Prove the executed code is the edited code (temporary marker log line, or printed module path + mtime), and re-run with build/transform caches cleared (jest --no-cache / clean build). Only then interpret.'

## [DEBUG.md | DEBUG.md] D8 clause (1) (line 13)
ISSUE: '(1) the ORIGINAL repro passing' restates D1's re-run requirement in the same file; harmless as reinforcement but the two wordings could drift.
FIX: Change to '(1) the D1 re-run (original command, unmodified) passing'.

## [EFFICIENCY.md | EFFICIENCY.md] Governing frame, line 4
ISSUE: 'These rules come in pairs — every "read less" rule has a "read enough" floor' is false as stated: six read-less rules (E1, E3, E11, E12, E13, E17) vs three floors (E2, E4, E14). E11, E12 and E17 have no paired floor. A model told 'every rule has a floor' either hunts for floors that do not exist or treats the floor list as an exhaustive license.
FIX: Replace the sentence with: 'Every "read less" rule is capped by the "read enough" floors below (E2, E4, E14). Never trade edit safety for token savings.' Optionally add inline pointers '(floor: E2)' to E1 and '(floor: E4)' to E3 so the pairing survives isolated citation.

## [EFFICIENCY.md | EFFICIENCY.md] Section IDs (lines 6-29) — the E1,E3,E11.. / E2,E4,E14 jumps
ISSUE: The non-sequential grouping is correct behavior under F12 (stable IDs, grouped by theme) and citation-by-ID works fine, but nothing says so. A later editor will be tempted to 'fix' the numbering (an F12 violation), and a reader may hunt for supposedly missing IDs between E7 and E10.
FIX: Append one clause to line 4 (or to '## The numbers in this file'): 'IDs are stable per docs/guardrails/_FORMAT.md F12; rules are grouped by theme, so numbering is intentionally non-sequential — never renumber.'

## [EFFICIENCY.md | EFFICIENCY.md] E17, line 12 (vs E1, line 7)
ISSUE: Threshold soup: E1 forbids full-file Reads of 300-400-line files unless 3+ scattered edits or a failed Grep, yet E17 casually presupposes such full-reads ('full-read at most ONCE'). Reconcilable, but three unexplained thresholds (250/300/400) across two adjacent rules invite exactly the which-side-am-I-on rationalization the reference section warns about.
FIX: Replace E17 with: '- E17. A full-file Read justified by E1 on a file >300 lines: at most ONCE per session; immediately record a 3-6 line structure map (key symbols + line ranges) in docs/STATE.md `## Facts`; every later visit is a ranged Read or Grep.'

## [EFFICIENCY.md | EFFICIENCY.md] E4, line 16
ISSUE: Two of E4's re-read triggers require judgment at the moment of action: 'a test script that may write files' ('may' per whose assessment?) and 'any git command touching the tree' (does status? diff? fetch?). F2/F14 want enumerable events.
FIX: Replace the middle of E4 with an enumeration: '...a formatter/linter/codegen run, any test or build run, any of git checkout/reset/stash/merge/rebase/pull/apply, a compaction, the user edited, or 20+ intervening tool calls.'

## [EFFICIENCY.md | EFFICIENCY.md] E11, line 9
ISSUE: 'count/files mode' is not the literal Grep parameter (F9 wants greppable tokens: output_mode count / files_with_matches), and '~50' hedges a number the same line states exactly as '>50'.
FIX: Replace E11 with: '- E11. Grep returned >50 hits? Do not scroll them or start opening hit files. Re-run with output_mode count or files_with_matches, then narrow (stricter pattern, glob/type filter, path). Still >50 relevant hits? It is a sweep — delegate (E8).'

## [EFFICIENCY.md | EFFICIENCY.md] E5-E6, lines 20-21 (vs CLAUDE.md iron rule, line 32)
ISSUE: E5+E6 restate CLAUDE.md's batching/narration iron rule ('Batch independent tool calls into one message; between calls write at most one line, findings and decisions only') — the same F7 single-source pattern as E14. No wording drift yet, but nothing prevents it; same resolution needed.
FIX: Resolve together with the E14 finding: declare in _FORMAT.md F7 that an iron rule may exist as a compressed always-on line whose operational form lives in exactly one doc, and list the three sanctioned pairs (iron 1/C1, iron 11/E14, iron 13/E5+E6); otherwise delete the doc-side copies.

## [EFFICIENCY.md | EFFICIENCY.md] Reference section, line 34 ('## Long task ahead, context already heavy')
ISSUE: Uses 'see docs/guardrails/SESSION.md' — F10 bans see/consult/check when a tool call is meant — and restates SESSION.md's '>200 lines -> log file' rule, which already lives in SESSION.md's update-trigger table (F7 duplication; and after the E10 fix it would exist in three places).
FIX: Replace the paragraph body with: 'Checkpoint before it is too late: Read docs/guardrails/SESSION.md and bring docs/STATE.md up to the present; prefer delegation (E8) for anything wide. Compaction after a checkpoint costs minutes; compaction before one costs the session.'

## [EFFICIENCY.md | EFFICIENCY.md] Reference section header, line 36
ISSUE: '## The numbers in this file' is a topic-noun header; F11 requires reference headers be second-person situations. Topic headers are exactly what F11's rationale says never fire.
FIX: Rename to: '## You are unsure which side of a threshold you are on' (body already matches that situation: 'When unsure which side of a threshold you are on, count.').

## [PLAN.md | PLAN.md] line 6 (P1, last sentence)
ISSUE: 'Widen only after that read disproves the premise' is too strict read literally: legitimate widening (reading the named function's caller to understand a contract) requires no disproof, so a compliant model either stalls or fabricates a 'disproof' to license the next Read. Also P1 carries no greppable literal token (F9).
FIX: Replace the last sentence with: 'Widen only after that read, and only with a stated one-line question the next file answers (docs/guardrails/EFFICIENCY.md E13); if the read disproved the premise, say what it disproved.'

## [PLAN.md | PLAN.md] line 13 (P4 CONSTRAINTS line)
ISSUE: The trigger-word list is 'don't / keep / only' — missing 'stop'. The CLAUDE.md iron rule and SESSION.md's update table both use the four-token set 'don't / only / keep / stop'. Inconsistent literal token lists across files invite drift and make grep-based compliance checks miss.
FIX: Change to: 'CONSTRAINTS: <verbatim every "don't / only / keep / stop" the user stated>'.

## [PLAN.md | PLAN.md] line 33 (Dependencies paragraph, last sentence)
ISSUE: 'Never install a package to work around an undiagnosed bug' has no ' -> instead: ' replacement — violates F5 and the kit bar item 4 (every NEVER carries a replacement).
FIX: Rewrite as: 'Never install a package to work around an undiagnosed bug -> instead: write the CAUSE line (docs/guardrails/DEBUG.md D3) first, then re-evaluate whether the dependency is still needed.'

## [PLAN.md | PLAN.md] line 15 (P5)
ISSUE: 'Run the relevant check once' — 'relevant' is a judgment word (F14). A mid-tier model will pick the cheapest check (lint) and call it a baseline, making later 'was that already broken?' comparisons useless for test failures.
FIX: Replace 'the relevant check' with an ordered concrete choice: 'the narrowest test command covering FILES (single test file or package), else the build, else lint'.

## [PLAN.md | PLAN.md] line 2 (trigger restatement)
ISSUE: The restated trigger says '(or no other routing row matched)' but the CLAUDE.md routing row it restates reads 'are unsure which row applies'. These are different conditions ('nothing matched' would route every trivial task here; 'unsure' routes only genuine ambiguity), and the kit's own discipline is verbatim restatement of triggers.
FIX: Change the parenthetical to match the routing table verbatim: '(or you are unsure which routing row applies)'.

## [SESSION.md | SESSION.md] line 41 (Failed-attempts row) + line 29 (template placeholder) vs DEBUG.md line 11 (D6)
ISSUE: The ATTEMPT format is stated three ways: D6 has `ATTEMPT n: <what changed> -> <exact observed failure>`; line 41 has `ATTEMPT n: <change> -> <failure>` (loses the 'exact observed' requirement); line 29's placeholder is a third variant with a meta-note '(during active debugging)' baked in. Paraphrase drift on a greppable artifact the kit forbids paraphrasing.
FIX: Line 41 cell: 'Failed attempts — exact format per docs/guardrails/DEBUG.md D6'. Line 29 placeholder: '<ATTEMPT entries per docs/guardrails/DEBUG.md D6>'.

## [SESSION.md | SESSION.md] line 37 (Constraints row) vs CLAUDE.md line 31 (iron rule)
ISSUE: The row restates CLAUDE.md's iron rule (append 'don't/only/keep/stop' verbatim to `## Constraints` in the same turn) instead of pointing to it — F7 restatement. Only the `[RECURRING]` extension is new material, and it is buried inside a triple-clause table cell where the 're-read Constraints before every edit' escalation is easy to miss.
FIX: Row cell: 'Constraints — per the CLAUDE.md iron rule; SECOND correction of the same behavior -> tag the bullet `[RECURRING]` and re-read `## Constraints` before every edit for the rest of the session'.

## [SESSION.md | SESSION.md] lines 5 and 8 vs CLAUDE.md line 46 (kit footer)
ISSUE: Improper duplication with the footer re-arm line. Line 8's 'Docs read before compaction no longer count as read' restates the footer verbatim minus the `(cached)` invalidation mechanics (a weaker paraphrase of the owning rule), and step 1 'Read docs/STATE.md in full' repeats the footer's identical command, mandating a double read of STATE.md in the same recovery turn.
FIX: Delete 'Docs read before compaction no longer count as read.' from line 8 (the footer owns it; keep only the unique UNVERIFIED-claims sentence). Append to step 1: '(already Read this turn per the CLAUDE.md footer? cite that Read — do not re-read)'.

## [SESSION.md | SESSION.md] lines 41, 51, 57 (cross-reference forms)
ISSUE: Cross-references use short names and a banned verb: '(see DEBUG.md D6)', '(CODE.md C4)', '(DEBUG.md D2)'. _FORMAT.md F10 requires every doc reference be the full literal path `docs/guardrails/<NAME>.md` and bans 'see'; a model that tries `Read DEBUG.md` from the project root gets file-not-found.
FIX: Replace with 'per docs/guardrails/DEBUG.md D6', 'docs/guardrails/CODE.md C4', 'docs/guardrails/DEBUG.md D2'. (Same defect exists at DEBUG.md line 31 '(CODE.md C3)' — fix kit-wide in one edit.)

## [SESSION.md | SESSION.md] lines 10-30 (template header vs placeholder lines)
ISSUE: 'EXACTLY these sections (copy verbatim)' conflicts with the template's content: a literalist copy pastes the angle-bracket placeholder lines — including the meta-note '(during active debugging)' on line 29 — into the live STATE.md. The headers must be verbatim; the bodies must not be.
FIX: Change line 10 to: 'create it with EXACTLY these nine `##` headers (headers verbatim; replace each `<...>` line with real content, delete unused placeholders)'.

## [SESSION.md | SESSION.md] line 2 (trigger restatement) vs CLAUDE.md line 14 (routing row)
ISSUE: Line 2 adds a third trigger — 'or you cannot recall how the session started' — that does not exist in the CLAUDE.md routing row, so it can never route anyone here; it is also a judgment state, not an observable event (F2). The doc's trigger line must mirror the router exactly or the extra clause is dead text.
FIX: Either delete the clause from line 2, or add its observable form to the CLAUDE.md routing row: 'or you cannot restate the user's original request in one line'.

## [SESSION.md | SESSION.md] reference-section headers, lines 47, 53, 56
ISSUE: Reference headers are topic statements, not second-person situations as F11 requires ('Session ledger keywords', 'Exact values decay', 'Environment beliefs expire'). Per the kit's own theory, topic headers never fire at the moment of action. (Moot for line 47 if the keywords are promoted above the divider per the S-ID finding.)
FIX: Rephrase as situations: line 53 -> 'You are about to type a port, path, command, or symbol from memory'; line 56 -> 'You are about to rely on environment state you have not verified in 15 messages'.

## [budget-audit | SESSION.md] line 8, final sentence, vs CLAUDE.md line 46 (footer re-arm line)
ISSUE: The sentence 'Docs read before compaction no longer count as read' appears in both the always-on CLAUDE.md footer and SESSION.md — an F7 restatement that double-pays tokens at exactly the post-compaction moment when both texts are guaranteed to be in context together.
FIX: Keep the CLAUDE.md footer copy (it must be always-on to re-arm routing) and delete the duplicate sentence from SESSION.md line 8, leaving: 'Treat every compaction-summary claim about file contents or completed work as UNVERIFIED until confirmed by a Read or git evidence.'

## [VERIFY.md | VERIFY.md] line 8 (V3)
ISSUE: 'quote every hit' is uncapped. A long build log with dozens of benign 'warn'/'error: 0' matches forces either a context-flooding paste (colliding with EFFICIENCY.md E10 'never print an entire log into context') or silent noncompliance, and the model will pick noncompliance.
FIX: Cap and disposition: 'quote up to the first 10 hits, each with a one-word disposition (benign/real), or state "failure-token scan: 0 hits"; more than 10 hits -> treat the run as not clean and stop pasting.'

## [VERIFY.md | VERIFY.md] line 6 (V1)
ISSUE: V1 is a meta-principle over all other items, so its echo line has no natural content — the format demands 'the command you ran + its last output line' but V1 names no command, and its freshness claim duplicates the evidence table's 'ran after last edit?' column. Models will emit a contentless 'V1: PASS'.
FIX: Specify its payload: 'V1. Fresh evidence: ... Your V1 line quotes the PRIMARY verification command run after the last edit and its output line; all other V-lines inherit freshness from it.'

## [VERIFY.md | VERIFY.md] line 10 (V5)
ISSUE: 'pip install -e' is listed as an example of 'ran the source entrypoint' — it is an editable install, not an entrypoint, and for packages with compiled extensions an editable install still requires a rebuild after the edit. Slightly wrong taxonomy invites 'I pip-installed -e last week, so V5 passes'.
FIX: Reword: '...or you ran the code from source (python -m, npx tsx, cargo run) or the package is installed editable (pip install -e — compiled extensions still need a rebuild).'

## [VERIFY.md | VERIFY.md] lines 32-33 (Windows CRLF section)
ISSUE: The remediation 'redo the edit preserving original endings' has no literal mechanism (weak on F9), and the obvious revert path (`git checkout -- <file>`) is gated by a CLAUDE.md hard stop the section doesn't acknowledge — a model may either freeze or violate the hard stop.
FIX: Add the concrete step: 'Remediate: paste the logic change you intend to keep, then `git checkout -- <file>` (this satisfies the CLAUDE.md hard stop by pasting what is lost), then re-apply with Edit using exact old_string from a fresh Read.'

## [VERIFY.md | VERIFY.md] line 36 (Todos section, last sentence)
ISSUE: 'record the RESULT, not just the activity' for STATE.md `## Done` entries restates SESSION.md (Done template line 25 and the 'Before an expensive operation' section, line 60) — a cross-file restatement rather than a pointer (F7).
FIX: End the section at 'never "complete"' and replace the rest with a pointer: 'Done-entry format: docs/guardrails/SESSION.md `## Done`.'

## [VERIFY.md | VERIFY.md] line 9 (V4)
ISSUE: '"It compiles" is gate 0, never completion evidence' conflicts with tasks whose PLAN.md P4 DONE-WHEN is itself the build/typecheck (e.g. 'make tsc pass'). For those, the passing typecheck IS the acceptance command, and 'never' forces the model to rationalize one rule against the other.
FIX: Add the carve-out: '...quote its result line — unless the TASK block's DONE-WHEN is the build/typecheck itself, in which case quote that passing output as the acceptance evidence.'

## [cross-doc | CLAUDE.md] Iron rule 3 (REFERENCE SWEEP trigger list) vs CODE.md C12
ISSUE: The always-on trigger list ('signature, name, key, or schema') is a paraphrased subset of C12's ('signature, symbol name, return shape, config key, route, CLI flag, env var, or enum member'). A route/env-var/CLI-flag change made when CODE.md is not freshly cached never fires the sweep.
FIX: Iron rule 3: 'After changing any signature, name, key, route, flag, env var, or enum member: run REFERENCE SWEEP per docs/guardrails/CODE.md (missed callers break silently).' — or copy C12's list byte-identically.

## [cross-doc | MIGRATE.md] M3 example table, row 003, vs M3's own disposition rule
ISSUE: M3 says SUPERSEDED-BY must 'name kit doc + rule ID', but the example the model is told to 'clone exactly' shows 'SUPERSEDED-BY | docs/guardrails/VERIFY.md echo protocol' — no rule ID. Models copy the example, not the rule.
FIX: Change the example destination to 'docs/guardrails/VERIFY.md V1-V3 (echo protocol)'.

## [cross-doc | MIGRATE.md] line 1 header ('Do not improvise, reorder, or batch steps') vs CLAUDE.md iron rule 13 / EFFICIENCY E5
ISSUE: In UPGRADE mode the kit CLAUDE.md is loaded, so 'batch independent tool calls into one message' (iron rule 13, E5) and 'do not batch steps' (MIGRATE header) both bind. 'Steps' vs 'tool calls' is distinguishable to a careful reader, but a mid-tier model will either batch M-phases or refuse to batch the independent verify commands inside one phase.
FIX: MIGRATE line 1: '...Do not improvise, reorder, or batch PHASES; batching independent tool calls inside a single numbered step is fine.'

## [cross-doc | MIGRATE.md] M6b composed-CLAUDE.md layout (line 1 + three zones)
ISSUE: The kit template's line 2 ('Editing this file? Read docs/guardrails/_FORMAT.md first. Never paraphrase kit text.') sits outside the KIT CORE markers, and M6b's layout (sentinel line 1, then zone 1 = marker block) silently drops it. Migrated projects lose the only pointer that routes future CLAUDE.md edits to the authoring contract — even though _FORMAT.md itself IS installed by M6a.
FIX: M6b: add 'line 2: copy line 2 of <KIT>/CLAUDE.md verbatim (the _FORMAT.md pointer)' between the sentinel and zone 1.

## [cross-doc | README.md] line 1 (missing version marker) and '## Upgrading the kit' (missing notes section F15 cites)
ISSUE: README is the only kit file without the `<!-- guardrails-kit: v1.0 -->` marker (it has only the heading text), so a marker-grep across the kit misses it. Additionally _FORMAT F15 requires every kit edit to be 'listed in the migration/upgrade notes of README.md' — no such notes list exists anywhere in README.
FIX: Insert '<!-- guardrails-kit: v1.0 -->' as README line 1, and add an '## Upgrade notes' section (e.g. 'v1.0 — initial release') under '## Upgrading the kit' so F15 has a real target.

## [cross-doc | README.md] '## Auditing compliance' token list
ISSUE: The audit list greps for 'TASK', but PLAN P4's block emits no literal 'TASK' token (its greppable artifacts are GOAL:/FILES:/DONE-WHEN:/CONSTRAINTS:). The list also omits several markers the kit mandates elsewhere: PLAN CHANGE:, ASSUMPTION:, DECISION:, RETURNING:, WORKAROUND:, HANDLED FAILURES:, and the C/D/E ID series — so the documented audit undercounts compliance.
FIX: Replace '`TASK`/`GOAL:`' with '`GOAL:`, `FILES:`, `DONE-WHEN:`' and extend the list with `PLAN CHANGE:`, `ASSUMPTION:`, `DECISION:`, `RETURNING:`, `WORKAROUND:`, `HANDLED FAILURES:`, `C1:`–`C14:`, `D1:`–`D10:`, `E1:`–`E17:` (and `S1:`+ once SESSION gains IDs).

## [cross-doc | CODE.md] C7 category list vs trap-table section headers; also README.md kit-table row for CODE.md
ISSUE: C7 tells the model to 'find your row' for categories that don't match the section names: 'range endpoints' has no section at all; 'negated conditions' maps only implicitly to 'Boolean logic'; '% or integer division' vs header 'Division & modulo'; the 'Familiar-API traps' section is not in C7's list. README's summary also omits division/modulo and familiar-API. Lookup misses default to the REPL-probe fallback at best, silence at worst.
FIX: Rewrite C7's list to quote the section headers verbatim ('Dates & times, Epochs & units, Mutation vs copy, Async, Floats & money, Sort, Division & modulo, Regex & strings, Familiar-API traps, Closures in loops, Boolean logic'), and either add a 'Range endpoints' row (or fold it into Dates' inclusivity line explicitly) or drop it from C7.

## [cross-doc | CODE.md] '## REFERENCE SWEEP' (above the divider, topic-noun header); plus topic-noun reference headers in VERIFY.md ('Forbidden phrases...', 'Evidence table', 'Todos and STATE.md entries'), DEBUG.md ('Red flags —...'), CODE.md ('Trap tables —...'), EFFICIENCY.md ('The numbers in this file')
ISSUE: F11 requires everything except the ID'd checklist to sit below '--- reference ---' with second-person situation headers. REFERENCE SWEEP sits above the divider under a topic-noun header, and six reference headers across four docs are topic nouns, not situations. The kit's own files break the contract they tell editors to follow.
FIX: Amend F11 to legalize the load-bearing case: 'Named procedures invoked by ID from the checklist or CLAUDE.md (e.g. REFERENCE SWEEP) may sit between the checklist and the divider under their invoked name.' Reword the remaining reference headers as situations, e.g. VERIFY: '## You are about to type "should work"', DEBUG: '## Your own words match a red-flag phrase'.

## [cross-doc | EFFICIENCY.md] line 2 trigger ('or context is filling'); docs/guardrails/SESSION.md line 2 ('or you cannot recall how the session started')
ISSUE: Both docs' line-1 triggers add conditions absent from the routing table, so they can never fire via routing; 'context is filling' and 'cannot recall' are judgment states, violating F2 and the kit's bar 2. F11 says line 1 RESTATES the trigger, not extends it.
FIX: Delete 'or context is filling' from EFFICIENCY line 2 and 'or you cannot recall how the session started' from SESSION line 2 (or add matching observable rows to the routing table, e.g. 'see a context-compaction warning').

## [cross-doc | EFFICIENCY.md] E10 example commands
ISSUE: E10's primary example mixes dialects: `cmd *> build.log` is PowerShell redirection paired with Select-String, with POSIX given only for the extraction step — the POSIX redirect form is missing. This contradicts E16 (one dialect per command) in the very file that states E16, and drops MIGRATE's `POSIX | PowerShell` presentation convention.
FIX: Give both complete forms in MIGRATE's convention: '`cmd > build.log 2>&1` then `grep -iE "error|fail" build.log | head -30` | `cmd *> build.log` then `Select-String -Path build.log -Pattern "error|fail" | Select-Object -First 30`'.

## [cross-doc | PLAN.md] P4 TASK block, CONSTRAINTS field
ISSUE: P4's capture list is '<verbatim every "don't / keep / only" the user stated>' — it omits 'stop', which both CLAUDE.md iron rule 12 and SESSION's update-trigger row include ('don't / only / keep / stop'). A constraint phrased with 'stop' gets written to STATE.md but not into the TASK block.
FIX: P4 CONSTRAINTS: '<verbatim every "don't / keep / only / stop" the user stated>'.

## [cross-doc | SESSION.md] '## Environment beliefs expire' vs DEBUG.md D2
ISSUE: The environment-reverification rule (15-message staleness, netstat/git branch/package checks) is fully stated in both DEBUG D2 and SESSION's reference section, each with its own command list plus a cross-pointer. F7 single-source violation; the two 15-message values agree today but are free to drift apart.
FIX: Make SESSION the owner (it is the belief-decay doc) and cut D2 to the pointer: 'D2. "It worked before / stopped working": step 1 is environment re-verification per docs/guardrails/SESSION.md "Environment beliefs expire", before touching code.'

## [cross-doc | _FORMAT.md] F11 ('line 1 restates the trigger') vs F15 ('version comment on line 1 of the touched file')
ISSUE: F11 and F15 both claim line 1: every guardrail doc actually has the version comment on line 1 and the trigger on line 2, so the kit's own files violate F11 as written.
FIX: F11: change 'line 1 restates the trigger' to 'the first non-comment line restates the trigger'.

# VERDICTS

## CLAUDE.md
CLAUDE.md passes its own arithmetic — 13/15 iron rules, 4/5 CAPS lines, kit core+footer 40/55 lines, all matching README's claims — and the routing-table concept, TRIGGER-line contract, and hard-stop format are genuinely well engineered. But it fails its own two hardest standards where they matter most: (1) single-sourcing is systematically broken — seven of thirteen iron rules restate doc content rather than point to it, and the twin copies have already drifted (word lists, forbidden-phrase sets, status formats), exactly the decay F7 exists to prevent; and (2) the route-then-announce contract has three real seams: a blocker where the 'first Edit of this session' row plus the footer's cache invalidation leaves no compliant path to ever re-read CODE.md after compaction, an act-before-read loophole created by the batching iron rule, and an unbounded '(cached) — obey from memory' branch that a mid-tier model will use to skip checklist items invisibly. Secondary but real: judgment words ('large', 'will outlive this sitting', 'unsure') survive in the always-loaded routing rows in violation of the kit's own F2/F14, and two of the four hard stops have weakened '-> instead:' clauses (a Windows-only replacement command in a portable kit; 'confirm' without 'with the user'). All fixes are small, local text edits; the blocker (row 2 wording) should land first since it silently disarms the entire CODE.md discipline for the post-compaction half of every long session — precisely the sessions the kit targets.

## PLAN.md
PLAN.md is structurally sound against the kit's format contract (correct divider, second-person reference headers, stable IDs, mostly numeric thresholds, well under the 120-line cap) and P4's escape hatch ("Cannot fill FILES yet? investigate, then post it") shows the authors anticipated the investigate-before-plan problem — but the doc has six substantive weaknesses. The two most consequential are execution-order problems the assignment specifically probed: the walk instruction demands one line per item "in your reply" while P3/P5 require tool runs and P4 defers itself to first-Edit, inviting fabricated compliance lines at task start; and the scope-escalation brake in the reference section triggers on "2x your TASK block estimate" when the TASK block records no estimate, so the kit's main anti-scope-creep rule can never fire deterministically. Two single-source (F7) violations — P2 restating a CLAUDE.md iron rule and the twin-definition section restating CODE.md C3, both with wording drift already visible — plus a judgment-state header ("You are tempted...") guarding the YAGNI/dependency rules, and a missing docs/STATE.md bootstrap that PLAN's own PLAN-CHANGE procedure depends on, round out the majors. Remaining findings are token-list and phrasing polish. Nothing is an outright contradiction that would produce wrong code, so no blockers; fix the two ordering/computability majors first, since they gut the doc's enforcement exactly at the moment it matters.

## CODE.md
CODE.md is dense, in budget (99 lines vs the ~120 target — not too long to read fully), and most checklist items are genuinely checkable, but it has one blocker and a cluster of majors concentrated exactly where the assignment pointed. The lifecycle split is mostly right except C14, a pre-deletion gate filed under "After each edit" whose own text says "greps first", and C11, whose language-independent corruption check (git diff) is trapped inside a Python-only scope, leaving all non-Python edits with no after-edit gate at all. REFERENCE SWEEP is neither unambiguous nor complete: step 1's "OLD identifier" is undefined for most of C12's triggers (signature/return-shape/enum changes where the name is unchanged), and the "paste every hit, disposition every hit, do not proceed" mandate collides head-on with EFFICIENCY.md E11/E8, which forbid scrolling >50-hit results and require delegating find-all-usages sweeps — a live inter-doc contradiction with no reconciliation. The trap tables are findable by category label, but C7's index doesn't match the headers: the Familiar-API table has no trigger token (and its confidence traps never hit the "unsure? probe" hatch, so it's effectively dead), "range endpoints" has no row, and "negated conditions" doesn't name its "Boolean logic" header. Rather than cutting length, move the test-shares-literals section to VERIFY.md (its trigger occurs in the debug/verify flow, and nothing routes to it here) and add the one-line C15 hook to make the rewrite section reachable; several dead-end Nevers (C5, C9) also need their mandated "-> instead:" exits, and the C4/SESSION.md CONSTRAINT CHECK format has already drifted between its two homes.

## DEBUG.md
DEBUG.md is structurally one of the kit's stronger docs — event-phrased trigger, ID'd checklist, paste verbs, correct divider, accurate C3/STATE.md cross-references — but it has one genuine contradiction and several rationalization holes exactly where a mid-tier model would exploit them. The blocker: D4's hard cap of 'full suite at most twice per task' collides with D8's mandatory post-fix suite run the moment a confirmation run fails and a second fix lands, producing a procedural deadlock in the single most common debugging scenario. The escalation ladder's levels are self-declared and uncountable at L3+ (investigation actions have no defined 'failure'), so it needs its floors tied to the checkable ATTEMPT count in the ledger; D1's 'cannot reproduce -> stop' is a zero-effort exit with no symptom-match requirement; D5's flake handling is keyed to whether the model typed a phrase, which is trivially gamed; and the red-flag table misses the four phrases stuck models actually emit ('different approach', 'pre-existing', 'add a sleep', 'caching issue'). Three single-source violations (D10 vs CLAUDE.md hard stops with already-divergent lists, D2 vs SESSION.md's environment block, the test-expectation clause vs V10) show drift starting. All fixes are local rewrites; no structural redesign needed.

## VERIFY.md
VERIFY.md is structurally sound (correct trigger restatement, ID'd checklist, divider, accurate pointers to docs/STATE.md sections that SESSION.md actually defines) and its evidence-table and forbidden-phrase machinery are the strongest anti-hallucination devices in the kit — but the enforcement core has three genuine breaks: the echo protocol's FAIL rule literally contradicts its own N/A option (and N/A needs no reason, making it the abusable escape the assignment predicted), the status vocabulary contradicts itself across V7/line-25/line-36 with four spellings of one token, and V8's `git diff --stat` is vacuous once changes are staged — silently defeating the scope audit in the most common commit flow. The pattern-completion risk is real: nothing ties a quoted 'output line' to an actual tool result in the transcript, and V2's verbatim example line is a ready-made fabrication template; one added sentence ('the quoted line must appear in a tool result in THIS turn') closes most of it. Coverage of the V-items is otherwise good (freshness, tests, artifacts, monorepos, scope, endpoints, triviality) with only mild overlaps (V1 vs the evidence table, V2 vs V3 on skips) and one rule conflict (V4's 'never completion evidence' vs typecheck-goal tasks); contradictions with DEBUG.md are absent in substance but V10 duplicates DEBUG's expectation-change rule wholesale, and the Forbidden-phrases section duplicates a CLAUDE.md iron rule with visible drift — both single-source violations. Fix the three blockers and the tool-result-anchoring sentence and this doc does its job; leave them and a mid-tier model can pass the whole checklist without running a single command.

## EFFICIENCY.md
EFFICIENCY.md is one of the kit's stronger docs — the read-less/read-enough architecture is sound, most rules carry real numbers and literal commands, and E2 correctly defers to CODE.md C1 (I found no conflict there). But it has one genuine contradiction and a cluster of drift/portability defects. The blocker: E15's 10-line final-answer cap collides head-on with VERIFY.md's mandatory 12-line V-echo in the same completion turn, handing a mid-tier model a rule-sanctioned reason to skip verification evidence. The majors: E3's absolute 'never Read after your own Edit' contradicts the iron rule's re-Read-after-failed-Edit procedure and never points to E4, whose event list is the only definition of 'scope changed' — so the read-less/read-enough pairing fails precisely at the isolated-citation moment it was designed for; the doc's line-2 trigger drifts from the routing table ('context is filling' is unroutable judgment prose, 'large file' is unnumbered); E10's flagship redirect `cmd *> build.log` silently glob-corrupts in bash; E16's Bash-tool/POSIX mapping is wrong on the kit's own primary (PowerShell-shelled) machine; and E14 duplicates a CLAUDE.md iron rule with an already-diverged tripwire word list, exposing an unresolved kit-wide question about whether iron-rule/doc duplication is sanctioned under F7. The ID jumps themselves are correct F12 behavior and fine for citation — they just need a one-clause note so a future editor doesn't renumber and a reader doesn't hunt for missing IDs. Fix the blocker and the five majors and this doc holds its weight.

## SESSION.md
SESSION.md's content is faithful to the research digest and its numbers are good (80 lines, 10/15 messages, >200 lines, depth 2), but it is the least format-compliant doc in the kit and has two structural holes that break it exactly when it matters. Blockers: the STATE.md lifecycle can dead-end — creation hangs on the judgment-state trigger 'task will outlive this sitting' that a mid-tier model won't fire, and the post-compaction procedure has no branch for a missing STATE.md; and the CONSTRAINT CHECK format contradicts CODE.md C4 while both demand exactness (SESSION drops the mandatory 'skipping/asking' disposition). Majors: the doc has no S1.. checklist IDs and parks its always-on ledger keywords (ANCHOR/DETOUR/DECISION) below the '--- reference ---' divider where the kit's own theory says they won't be obeyed; the update-trigger table lost the 'before any /compact' trigger present in the source research; ANCHOR fires on the judgment phrase 'major step'; and 'Environment beliefs expire' restates DEBUG.md D2 with drift already visible. The remaining issues are single-source paraphrase drift (ATTEMPT format in three variants, Constraints row duplicating the CLAUDE.md iron rule, line 8 duplicating the footer re-arm), short-path 'see DEBUG.md' cross-references violating F10, a 'copy verbatim' template instruction that would paste placeholder meta-text into the live file, and a DECISION: keyword missing from README's audit grep list. Fixing the two blockers plus promoting the keywords into an S-ID'd checklist would bring the doc up to the rest of the kit's standard.

## MIGRATE.md
MIGRATE.md is the strongest-conceived file in the kit — the transport-not-authorship principle, line-accounting table, and echo-style Verify lines are exactly right — but execution-walking it exposes one genuinely destructive bug and a cluster of self-contradictions a mid-tier model must rationalize around. The blocker: M6a's per-file collision gate is followed by a wildcard `cp *.md` that overwrites files the user just chose to keep. The Phase-5 STOP gate is correctly PLACED (nothing destructive precedes it) but its 'before writing ANY new file' wording is violated by M2/M3/M4, teaching the model the gate is figurative. Several Verifies cannot run as written (M8 item 7 has no baseline hash from M2; M1's POSIX `test -e` prints nothing; M8 item 2 breaks on lines under 15 chars), the `git hash-object` checks need `--no-filters` to survive Windows CRLF normalization, and M3's exclusion of headings silently loses rule-bearing headings with no DROPPED trace. The fresh-install branch half-works: the copy/hash steps are sufficient, but M9 is literally unsatisfiable on that path ('all 8 checks') and skipping Phase 1 leaves nested CLAUDE.md/hook surfaces uninventoried. UPGRADE mode is the weakest section — no backup, no explicit approval wait, no post-swap verification, misattributes ordinary version drift to hand-editing, and U3 instructs editing freshly copied kit docs (breaking next-upgrade hash parity). All fixes are local rewordings; none require redesign.

## cross-doc
The kit is structurally coherent — all rule-ID cross-references (C1, C3, C4, D2, D6, E8, D3) resolve, all cited paths exist and are spelled identically, the STATE.md section names match the SESSION.md template exactly everywhere they are cited, and the six guardrail docs carry identical version markers. But judged against its own bar it has two blockers and a cluster of majors: the done/unverified status grammar is mandated in four mutually inconsistent literal forms across VERIFY.md, CLAUDE.md, and DEBUG.md (breaking the greppable-compliance premise), and MIGRATE's fresh-install and upgrade paths are literally unexecutable (M9's all-8-checks gate cannot pass in fresh-install mode; U1 hash-compares project-owned files that have no kit source). The deepest systemic issue is that CLAUDE.md's iron rules restate — with already-visible wording drift — six rules the docs also own, directly violating the kit's own F7 single-source contract, and _FORMAT itself contains contracts the kit's files don't meet (F5's ' -> instead: ' token appears only in the 4 hard stops, F11's line-1 trigger collides with F15's line-1 version comment, F12's S-series IDs exist nowhere because SESSION.md has no checklist IDs at all). The remaining minors are grammar/lookup drift (CONSTRAINT CHECK tail, C7 category names vs trap-table headers, P4's missing 'stop', README's audit-token list) that individually cost little but collectively erode the exactness the kit sells. All fixes are one-to-three-line edits; nothing requires restructuring.

## routing-sim
The routing table handles the easy scenarios: S3 (pytest exit 1 -> row 3 -> DEBUG.md, fires immediately and unambiguously), S4 (literal "Done"/"fixed" -> row 4 -> VERIFY.md, though only because the exact tokens appear), and S6's row-6 match is clear — but S6 then hits a genuine contradiction, because the footer orders Read docs/STATE.md as the next tool call while the routing contract orders Read SESSION.md, and post-compaction row 2 can never re-fire despite the cache being invalidated. S1 exposes the worst structural gap: for a user-reported crash no failure exists in the transcript, so DEBUG.md is unreachable and the model routes to CODE.md at first Edit and patches unreproduced — the kit's debugging procedure cannot fire at the one moment it exists for. S2 works only if the model correctly forecasts ">2 file edits" at start; discovered-mid-task scope growth misses the literal "start a task" moment, "component" is an undefined judgment word, and a simultaneous rows-1+2 match has no ordering rule while row 7's "unsure which row applies" actively misroutes that confusion to PLAN.md. S5 depends on an undefined "large" and an implicit running counter a mid-tier model will not keep. The adversarial probes all found real holes: the "(cached)" hatch never expires within an uncompacted session and "obey from memory" leaves no transcript evidence; the contract's only NEVER polices the TRIGGER line but not the Read, so line-without-Read is technically compliant; Bash-based file writes bypass row 2 entirely; and EFFICIENCY.md/SESSION.md line-1 restatements carry extra trigger clauses ("context is filling", "cannot recall how the session started") that exist nowhere in the router and are therefore dead. Verdict: the routing architecture is sound and mostly checkable, but it needs the two blockers fixed (user-reported-bug entry into DEBUG.md; footer/contract post-compaction ordering) plus tightened event wording, a multi-match rule, and symmetric TRIGGER-line/Read enforcement before a mid-tier model will reliably hit the right doc at the right moment.

## migration-dry-run
Dry-run executed M0-M9 as a literal Opus against the MyShop legacy file. M0: no sentinel, proceed. M1 surfaces: root CLAUDE.md (MIGRATE), packages/worker/CLAUDE.md (LEAVE — but the 'unless they conflict' condition is unexecutable), .claude/settings.json PostToolUse hook (LEAVE; wording only fits injection hooks), @docs/api-conventions.md (missed — M1 never looks for imports), team docs/guardrails/VERIFY.md (not discovered until M6a, after the M5 approval gate). M2: snapshot, 13 lines. M3: 12 numbered lines, 12 rows — 001 dev-cmd/port MOVED->## Project; 002 runInBand test cmd MOVED->## Project (M4(a) fact); 003 NEVER-deploy-fridays KEPT->## Project; 004 lint-before-commit should be KEPT (kit has NO lint rule) but example row 003 instructs a rule-killing SUPERSEDED-BY on this exact text; 005 migrations rule KEPT->## Project; 006 zod KEPT->## Project; 007 legacy-auth DO-NOT-DELETE KEPT->## Project (example row 005 primes misrouting it to UNSORTED); 008 email-TODO UNSORTED->PROJECT-NOTES.md; 009 NEVER-integration-locally has no legal disposition (conflict-pending state missing); 010 style and 011 EPERM-lore MOVED->PROJECT.md, which nothing in the kit ever routes to; 012 @import undefined — moving it silently unloads api-conventions. Conflicts: (1) 009 vs VERIFY.md V1/V2/V4 + DEBUG.md D8 run-the-tests — caught, it is literally M4(c)'s worked example, escalated to the user at M5; (2) 011 'just rerun it' vs DEBUG.md D5's ban on unchanged re-runs — MISSED, zero M4 trigger tokens; plus 002's 'parallel = corruption' delta vs D4/D8 self-composed test commands, missed the same way, and a live caps-budget contradiction (F4's <=5 caps lines vs verbatim transport of 2-3 project NEVERs). The VERIFY.md collision is handled at M6a's precheck (hash differ -> quote 10 lines -> ask a/b/c) but is then defeated by the bulk `cp *.md` that overwrites regardless, is invisible to the user at the M5 checkpoint, breaks M8(3)/U1 forever if the user picks skip, and options a/c lack naming conventions. Verdict: the skeleton — snapshot, line-accounted transport, verbatim discipline, user gate, hash verification — is genuinely strong and would preserve most of this file; but four blockers (bulk-copy clobbering the team's VERIFY.md, the factually-wrong lint example that deletes a real rule by lookalike, the @import black hole, and the unreachable PROJECT.md destination) mean a diligent literal execution today loses two always-loaded rules, one auto-loaded doc, and possibly the team's release checklist while every M8 check still passes.

## fact-check
The CODE.md trap tables are factually very solid: of ~40 concrete claims, I probe-verified nearly all against Python 3.14.4 and Node 24.14.1 and they check out exactly as written — [10,9,1].sort() -> [1,10,9]; ['1','7','11'].map(parseInt) -> [1, NaN, 3]; Array(3).map holes; new Date('2024-01-15') = UTC midnight vs new Date(2024,0,15) = local (getDate() -> 14 vs 15 on this UTC-7 machine); replace() first-only; reused /g lastIndex statefulness; '$$' -> '$'; structuredClone preserving Dates/undefined vs JSON.stringify dropping them and nulling NaN/Infinity; toFixed -> string; 2**53 === 2**53+1; typeof null; -1%5 = 4 (Python) vs -1 (JS); ((x%n)+n)%n = 4; re.match vs fullmatch (including the '$ matches before \\n' case); str.split() vs split(' '); int-interning (100 is -> True, 1000 is -> False); mutable-default persistence; [[0]*n]*m aliasing; lambda i=i pinning; var/let closures; utcnow() naive (+DeprecationWarning) and naive<aware TypeError; epoch digit counts (time.time() 10 digits, Date.now() 13); py_compile exit 1 on syntax error; the inspect.signature one-liner; and the knowledge-verified JWT-exp-seconds, currentTimeMillis, Go Unix()/pre-1.22 loop capture, and Rust/Go/C/Java truncating % are all correct. The kit contains no lo+(hi-lo)/2 claim, and DEBUG/EFFICIENCY/CLAUDE.md tool syntax (netstat|findstr, pytest -x/--cache-clear, jest -t/--no-cache/-u, *> redirection, Select-String) is all valid. Three real defects stand out: (1) BLOCKER — the 'yyyy, never YYYY' row is inverted for Moment/Day.js, where YYYY is the correct calendar-year token and lowercase yyyy prints literally in Day.js (probed: 'yyyy-01-15'); it holds only for LDML-token libraries (Java, date-fns, Luxon) and must be scoped by library family. (2) MAJOR — 'reversed() returns new' is false: it is a lazy iterator over the live list (probed: mutation leaks through, [3,2,99]). (3) MAJOR — VERIFY V3's '$LASTEXITCODE = true exit code when piped' is wrong in PowerShell whenever the consumer is a native command (probed: exit-3 producer | findstr -> $LASTEXITCODE=1), which can certify a false PASS at the exact verification gate; plus three minor polish items (U+2212 minus signs that break copy-paste probes, [...str] grapheme-cluster caveat, unstated ES2023/Node-version floors for toSorted/structuredClone).

## budget-audit
Budget compliance is clean at the CLAUDE.md layer: 47 total lines (4,469 chars, ~1,100-1,300 always-on tokens), kit core+footer 40 lines vs the 55 cap, 13/15 iron rules, 4/5 all-caps hard-stop lines, 7 routing rows; all iron rules measure 18-26 words, so none need tightening (the only over-long always-on lines are the 52-word routing protocol at line 17 and the justified 39-word hard stop 1). All seven guardrail docs are under the ~120-line target (23-98 lines), so there are no line-count overruns to cut — but the line budget is being satisfied by packing: PLAN.md (36 lines, 23.2 words/line) outweighs SESSION.md (60 lines, 12.7 w/line) in tokens, and DEBUG D6 crams 98 words and six obligations into one ID. The two real cost problems are structural: CODE.md, the doc triggered in essentially every coding session, is also the heaviest (~2,600 tokens, 60% of it trap tables including languages the repo may not use — split them out per F11), and iron rule line 29 pays always-on rent for a mid-edit trap that C7 already points at but whose trap-table row doesn't exist (a dangling pointer the demotion fixes). One-time doc costs are otherwise proportionate to trigger frequency (~1,000-1,400 tokens each; MIGRATE.md ~3,000 install-only; worst-case full-kit session load ~8-9k tokens). No blockers found in this lane; the fixes above trim ~1,700 tokens off the every-coding-session load and close the one measurement loophole (lines-not-tokens) the kit's own format contract leaves open.
